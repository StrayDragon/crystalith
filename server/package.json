{
  "name": "@crystalith/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "build": "bun build --compile --outfile=crystalith-server ./src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'TODO: server lint'",
    "format": "echo 'TODO: server format'",
    "format:check": "echo 'TODO: server format:check'"
  },
  "dependencies": {
    "@crystalith/shared": "workspace:*",
    "elysia": "^1.4.29",
    "@elysiajs/eden": "^1.4.9"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
