## 1. 组合模型与配置契约

- [x] 1.1 定义核心服务与可选子服务的数据模型（enabled/endpoint/probe/degrade）。
- [x] 1.1a 明确规范名 `frontend/backend` 到 Compose 服务名 `web/api` 的映射并写入文档。
- [x] 1.2 更新 `config/app.schema.json` 与 `config/app.yaml` 注释，支持可选服务显式配置块。
- [x] 1.3 更新 `.env.example`，按“核心必需 + 可选服务”分组环境变量。

## 2. Compose 分层重构（原生主线）

- [x] 2.1 建立“核心基线 + 可选增强 + 环境覆盖”目录与命名规范。
- [x] 2.2 重构 `deployments/` compose 清单：默认仅核心服务，可选服务通过 overlay/profile 显式启用。
- [x] 2.3 统一 merge-first 规则（`-f` + overlays）；明确 `include` 的使用边界与禁用场景。
- [x] 2.4 使用 `x-` 扩展字段与 anchors 收敛重复片段（healthcheck/logging/resources/env）。

## 3. 启动入口与兼容迁移

- [x] 3.1 重构 Procfile/本地开发入口，使默认仅启动 frontend+backend。
- [x] 3.2 一步到位替换旧入口调用为新入口，并补充迁移指引（不保留兼容层）。
- [x] 3.3 统一本地与部署的组合命名和语义（overlay/profile），避免双套术语。

## 4. 后端依赖探活与降级语义

- [x] 4.1 统一可选服务探活器接口，输出服务状态与最后探活时间。
- [x] 4.2 为可选服务不可用场景统一错误码和用户可恢复提示。
- [x] 4.3 确保可选服务失败不影响核心路由可用性。

## 5. 可观测与验收能力

- [x] 5.1 提供组合状态查询能力（核心状态 + 可选服务状态）。
- [x] 5.2 编写核心验收脚本：core-only 启动、可选服务后启动恢复、外部服务接入。
- [x] 5.3 将核心 smoke 用例纳入 CI（至少覆盖 core-only 与 single-optional）。

## 6. 文档与开发体验

- [x] 6.1 更新 `deployments/README.md`：最小部署、可选增强、自托管替换路径。
- [x] 6.2 更新开发文档：本地启动矩阵、故障排查、常见 profile 组合。
- [x] 6.3 给前端错误提示文案加分类（核心故障 vs 可选服务故障）。

## 7. 评审关口（实现前）

- [x] 7.1 输出重构后行为变化清单（逻辑、配置、UI 提示、运维命令差异）。
- [x] 7.2 输出迁移说明（旧命令到新命令映射、回滚路径）。
- [x] 7.3 完成你方 review 后再进入实现阶段；第 5 节验收项在实现后执行，本阶段不执行容器编排命令。
