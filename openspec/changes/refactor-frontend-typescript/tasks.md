## 1. Implementation
- [x] 1.1 Add Vite + TypeScript dependencies and `tsconfig.json`.
- [x] 1.2 Add `vite.config.ts` and migrate `index.html` to the Vite root.
- [x] 1.3 Replace CRA scripts with Vite dev/build/test scripts.
- [x] 1.4 Convert entry/test utilities to `.ts`/`.tsx` and update imports.
- [x] 1.5 Restructure `src/` into `app/`, `features/`, `shared/` and move assets/styles.
- [x] 1.6 Split the workspace page into typed components with colocated styles.
- [x] 1.7 Add typed API helpers and shared domain types.
- [x] 1.8 Update `AGENTS.md` with the new frontend structure and commands.

## 2. Validation
- [x] 2.1 Run `pnpm test`.
- [x] 2.2 Run `pnpm run build`.
