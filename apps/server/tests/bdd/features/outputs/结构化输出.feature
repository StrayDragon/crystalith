# language: zh-CN

功能: 结构化输出
  作为用户
  我希望从来源文档生成结构化输出
  以便以不同格式呈现文档内容

  背景:
    假如 已启动应用
    而且 一个名为"输出测试笔记本"的笔记本

  场景: 生成段落输出
    假如 笔记本中有一篇来源"段落来源.md"
    当 生成"PARAGRAPH"类型输出
    那么 响应状态码为201
    而且 响应中"type"的值为"PARAGRAPH"

  场景: 生成要点输出
    假如 笔记本中有一篇来源"要点来源.md"
    当 生成"BULLETS"类型输出
    那么 响应状态码为201
    而且 响应中"type"的值为"BULLETS"

  场景: 生成结构化输出
    假如 笔记本中有一篇来源"结构化来源.md"
    当 生成"STRUCTURED"类型输出
    那么 响应状态码为201
    而且 响应中"type"的值为"STRUCTURED"

  场景: SLIDES类型被拒绝
    假如 笔记本中有一篇来源"幻灯片来源.md"
    当 生成"SLIDES"类型输出
    那么 响应状态码为400

  场景: 插件依赖类型缺少插件时返回409
    假如 笔记本中有一篇来源"插件来源.md"
    当 生成"FAQ"类型输出
    那么 响应状态码为409
    而且 响应错误码为"OUTPUT_TYPE_PLUGIN_REQUIRED"

  场景: 列出笔记本的所有输出
    假如 笔记本中有一篇来源"列表来源.md"
    而且 已生成一个"PARAGRAPH"类型输出
    当 请求输出列表
    那么 响应状态码为200
    而且 响应列表包含1条记录

  场景: 获取单个输出详情
    假如 笔记本中有一篇来源"详情来源.md"
    而且 已生成一个"PARAGRAPH"类型输出
    当 请求最新输出的详情
    那么 响应状态码为200
    而且 响应中"type"的值为"PARAGRAPH"

  场景: 导出输出为Markdown格式
    假如 笔记本中有一篇来源"导出来源.md"
    而且 已生成一个"PARAGRAPH"类型输出
    当 导出最新输出为"markdown"格式
    那么 响应状态码为200

  场景: 导出输出为JSON格式
    假如 笔记本中有一篇来源"导出来源.md"
    而且 已生成一个"PARAGRAPH"类型输出
    当 导出最新输出为"json"格式
    那么 响应状态码为200

  场景: 删除输出
    假如 笔记本中有一篇来源"删除来源.md"
    而且 已生成一个"PARAGRAPH"类型输出
    当 删除最新输出
    那么 响应状态码为204

  场景: 将输出转换为来源
    假如 笔记本中有一篇来源"转换来源.md"
    而且 已生成一个"PARAGRAPH"类型输出
    当 将最新输出转换为来源
    那么 响应状态码为201
    而且 响应中"source_id"的值大于0
