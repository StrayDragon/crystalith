# language: zh-CN
功能: 命令系统
  作为一个知识工作者
  我希望能查看可用命令
  以便通过触发词快速执行预设操作

  场景: 列出所有命令
    假如 已启动应用
    当 请求命令列表
    那么 响应状态码为200
    而且 响应列表至少包含1条记录

  场景: 包含内置预设命令
    假如 已启动应用
    当 请求命令列表
    那么 响应状态码为200
    而且 响应中存在来源为"builtin"的命令

  场景: 命令按触发词排序
    假如 已启动应用
    当 请求命令列表
    那么 响应状态码为200
    而且 响应列表按"trigger"升序排列
