# language: zh-CN
功能: 来源标签
  作为一个用户
  我希望能用标签分类来源
  以便快速筛选和组织文档

  场景: 创建标签
    假如 一个空白笔记本
    当 创建一个名为"重要"的来源标签
    那么 响应状态码为201
    而且 响应中"name"的值为"重要"

  场景: 列出标签
    假如 一个空白笔记本
    假如 已存在来源标签"标签A"
    假如 已存在来源标签"标签B"
    当 请求当前笔记本的标签列表
    那么 响应状态码为200
    而且 响应列表至少包含2条记录

  场景: 重命名标签
    假如 一个空白笔记本
    假如 已存在来源标签"旧名称"
    当 将来源标签重命名为"新名称"
    那么 响应状态码为200
    而且 响应中"name"的值为"新名称"

  场景: 删除标签
    假如 一个空白笔记本
    假如 已存在来源标签"待删除"
    当 删除该来源标签
    那么 响应状态码为204

  场景: 重复标签名返回409
    假如 一个空白笔记本
    假如 已存在来源标签"重复"
    当 创建一个名为"重复"的来源标签
    那么 响应状态码为409

  场景: 将标签分配给来源
    假如 一个空白笔记本
    假如 笔记本中有一篇来源"文档.md"
    假如 已存在来源标签"分类"
    当 将该标签分配给当前来源
    那么 响应状态码为200

  场景: 从来源移除标签
    假如 一个空白笔记本
    假如 笔记本中有一篇来源"文档.md"
    假如 已存在来源标签"分类"
    假如 该标签已分配给当前来源
    当 从当前来源移除该标签
    那么 响应状态码为200
