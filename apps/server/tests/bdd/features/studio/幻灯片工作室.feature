# language: zh-CN
功能: 幻灯片工作室
  作为一个用户
  我希望能够创建和管理幻灯片演示
  以便从笔记本内容生成演示文稿

  背景:
    假如 已启动应用
    并且 一个名为"幻灯片测试笔记本"的笔记本
    并且 笔记本中有一篇来源"测试文档.md"

  场景: 创建幻灯片草稿
    当 创建幻灯片草稿，标题为"测试演示"，提示为"生成演示内容"
    那么 响应状态码为201
    并且 响应中"title"的值为"测试演示"
    并且 响应中"stage"的值为"input"
    并且 响应中"status"的值为"idle"

  场景: 获取幻灯片草稿
    假如 已创建幻灯片草稿"获取测试"
    当 获取幻灯片草稿详情
    那么 响应状态码为200
    并且 响应中"title"的值为"获取测试"

  场景: 更新幻灯片草稿
    假如 已创建幻灯片草稿"更新测试"
    当 更新幻灯片草稿，标题改为"更新后的演示"
    那么 响应状态码为200
    并且 响应中"title"的值为"更新后的演示"

  场景: 更新幻灯片大纲
    假如 已创建幻灯片草稿"大纲测试"
    当 更新幻灯片大纲
    那么 响应状态码为200
    并且 响应中"stage"的值为"outline"

  场景: 更新幻灯片 Markdown
    假如 已创建幻灯片草稿"Markdown测试"
    当 更新幻灯片Markdown为"# 测试标题\n\n测试内容"
    那么 响应状态码为200
    并且 响应中"stage"的值为"markdown"

  场景: 不存在的幻灯片草稿返回404
    当 发送 GET 请求"/v2/notebooks/{当前笔记本[id]}/slides/drafts/99999"
    那么 响应状态码为404

  场景: 无来源时无法创建草稿
    当 发送 POST 请求"/v2/notebooks/{当前笔记本[id]}/slides/drafts"，内容为：
      """json
      {"title": "空来源测试", "source_ids": []}
      """
    那么 响应状态码为400

  场景: 获取最新草稿
    假如 已创建幻灯片草稿"最新草稿测试"
    当 发送 GET 请求"/v2/notebooks/{当前笔记本[id]}/slides/drafts/latest"
    那么 响应状态码为200
    并且 响应中"title"的值为"最新草稿测试"
