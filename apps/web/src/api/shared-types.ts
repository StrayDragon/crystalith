/**
 * Shared API types — shrinking island (see `_PROGRESS.md` P1.4).
 *
 * Prefer Eden `treaty<App>` + `@crystalith/shared`.
 * Sources / research / tool descriptors migrated off this file.
 * Remaining: diagnostics-only placeholders until P1.4 diagnostics.
 */

// biome-ignore lint/suspicious/noExplicitAny: dynamic v2 response shapes
export type WorkspaceToolsDiagnostics = Record<string, any>;
