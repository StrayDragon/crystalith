# language: zh-CN
功能: 并发控制
  作为系统基础设施
  我希望能控制并发访问
  以便保护有限资源不被过度占用

  场景: 获取和释放限制器
    假如 一个限制为2的并发限制器"test"
    当 获取限制器"test"的许可
    那么 许可的限制为2
    而且 限制器快照中"acquires"为1

  场景: 限制为0表示无限制
    假如 一个限制为0的并发限制器"free"
    当 获取限制器"free"的许可
    那么 许可的限制为空

  场景: 指标快照
    假如 一个限制为1的并发限制器"metric"
    当 获取限制器"metric"的许可
    当 获取限制器"metric"的许可
    那么 限制器快照中"acquires"为2
    而且 限制器快照中"limit"为1

  场景: 多个独立限制器
    假如 一个限制为3的并发限制器"alpha"
    假如 一个限制为5的并发限制器"beta"
    当 获取限制器"alpha"的许可
    当 获取限制器"beta"的许可
    那么 限制器"alpha"快照中"limit"为3
    而且 限制器"beta"快照中"limit"为5
