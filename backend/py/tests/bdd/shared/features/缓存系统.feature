# language: zh-CN
功能: 缓存系统
  作为系统基础设施
  我希望能高效缓存数据
  以便减少重复计算和外部调用

  场景: 基本的存取操作
    假如 一个空的内存缓存
    当 缓存设置键"key1"的值为"hello"
    当 缓存获取键"key1"
    那么 缓存返回值"hello"

  场景: 获取不存在的键
    假如 一个空的内存缓存
    当 缓存获取键"不存在"
    那么 缓存返回空值

  场景: TTL 过期
    假如 一个空的内存缓存
    当 缓存设置键"ttl_key"的值为"temp"，TTL为0.05秒
    当 等待0.1秒
    当 缓存获取键"ttl_key"
    那么 缓存返回空值

  场景: 模式批量失效
    假如 一个空的内存缓存
    当 缓存设置键"user:1:name"的值为"张三"
    当 缓存设置键"user:2:name"的值为"李四"
    当 缓存设置键"post:1:title"的值为"文章"
    当 缓存按模式"user:*"失效
    那么 缓存中键"user:1:name"不存在
    而且 缓存中键"user:2:name"不存在
    而且 缓存中键"post:1:title"存在

  场景: 原子递增
    假如 一个空的内存缓存
    当 缓存原子递增键"counter"，增量为1
    当 缓存原子递增键"counter"，增量为5
    当 缓存获取键"counter"
    那么 缓存返回整数值6

  场景: 批量操作
    假如 一个空的内存缓存
    当 缓存批量设置键"a"的值为"1"、键"b"的值为"2"、键"c"的值为"3"
    当 缓存批量获取键"a"、"b"、"c"
    那么 缓存批量返回"1"、"2"、"3"

  场景: 删除键
    假如 一个空的内存缓存
    当 缓存设置键"to_delete"的值为"bye"
    当 缓存删除键"to_delete"
    当 缓存获取键"to_delete"
    那么 缓存返回空值
