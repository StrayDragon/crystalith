# language: zh-CN

功能: 内容精炼
  作为用户
  我希望将来源文档精炼为不同格式
  以便快速获取结构化摘要

  背景:
    假如 已启动应用
    而且 一个名为"精炼测试笔记本"的笔记本

  场景: 段落格式精炼
    假如 笔记本中有一篇来源"精炼来源.md"
    当 使用来源精炼内容为"paragraph"格式
    那么 响应状态码为200
    而且 响应中"format"的值为"paragraph"
    而且 响应中"evidence"为真

  场景: 要点格式精炼
    假如 笔记本中有一篇来源"精炼来源.md"
    当 使用来源精炼内容为"bullets"格式
    那么 响应状态码为200
    而且 响应中"format"的值为"bullets"
    而且 响应中"evidence"为真

  场景: 结构化格式精炼
    假如 笔记本中有一篇来源"精炼来源.md"
    当 使用来源精炼内容为"structured"格式
    那么 响应状态码为200
    而且 响应中"format"的值为"structured"
    而且 响应中"evidence"为真

  场景: 批量精炼多种格式
    假如 笔记本中有一篇来源"批量精炼来源.md"
    当 批量精炼内容为 "paragraph", "bullets", "structured" 格式
    那么 响应状态码为200
    而且 响应中"evidence"为真

  场景: 不支持的格式返回400
    假如 笔记本中有一篇来源"精炼来源.md"
    当 使用来源精炼内容为"unsupported"格式
    那么 响应状态码为400

  场景: 无来源时精炼返回空引用
    假如 笔记本中无来源
    当 无来源精炼内容为"paragraph"格式
    那么 响应状态码为200
    而且 响应中"evidence"为假
