# language: zh-CN
功能: 模板管理
  作为一个知识工作者
  我希望能使用和管理笔记本模板
  以便快速创建结构化的笔记本

  场景: 内置模板已预置
    假如 已启动应用
    当 请求模板列表
    那么 响应状态码为200
    而且 响应列表至少包含1条记录
    而且 响应中存在内置模板

  场景: 创建自定义模板
    假如 已启动应用
    当 创建一个名为"我的模板"的模板
    那么 响应状态码为201
    而且 响应中"name"的值为"我的模板"
    而且 响应中"is_builtin"为假

  场景: 获取模板详情
    假如 存在自定义模板"详情模板"
    当 请求该模板详情
    那么 响应状态码为200
    而且 响应中"name"的值为"详情模板"

  场景: 更新自定义模板
    假如 存在自定义模板"旧模板名"
    当 将模板重命名为"新模板名"
    那么 响应状态码为200
    而且 响应中"name"的值为"新模板名"

  场景: 删除自定义模板
    假如 存在自定义模板"待删除模板"
    当 删除该模板
    那么 响应状态码为204

  场景: 不能修改内置模板
    假如 存在内置模板
    当 尝试修改内置模板名称为"被拒绝的名称"
    那么 响应状态码为409

  场景: 不能删除内置模板
    假如 存在内置模板
    当 尝试删除内置模板
    那么 响应状态码为409

  场景: 将笔记本保存为模板
    假如 一个名为"原始笔记本"的笔记本
    当 将笔记本保存为模板"导出模板"
    那么 响应状态码为201
    而且 响应中"name"的值为"导出模板"
    而且 响应中"is_builtin"为假
