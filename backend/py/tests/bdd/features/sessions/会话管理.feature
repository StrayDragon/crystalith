# language: zh-CN
功能: 会话管理
  作为一个用户
  我希望能管理对话会话
  以便组织不同主题的讨论

  场景: 创建会话
    假如 一个空白笔记本
    当 创建一个名为"新会话"的会话
    那么 响应状态码为201

  场景: 列出会话
    假如 一个空白笔记本
    假如 一个名为"会话A"的会话
    当 请求当前笔记本的会话列表
    那么 响应状态码为200
    而且 响应列表至少包含1条记录

  场景: 更新会话标题
    假如 一个空白笔记本
    假如 一个名为"旧标题"的会话
    当 将会话标题更新为"新标题"
    那么 响应状态码为200
    而且 响应中"title"的值为"新标题"

  场景: 删除会话
    假如 一个空白笔记本
    假如 一个名为"待删除"的会话
    当 删除当前会话
    那么 响应状态码为204

  场景: 跨笔记本访问会话返回404
    假如 一个名为"笔记本A"的笔记本
    假如 一个名为"会话A"的会话
    假如 一个名为"笔记本B"的笔记本
    当 从另一个笔记本访问该会话
    那么 响应状态码为404
