# language: zh-CN
功能: 消息管理
  作为一个用户
  我希望能在会话中发送和查看消息
  以便进行对话交流

  场景: 创建消息
    假如 一个空白笔记本
    假如 一个空白会话
    当 发送一条用户消息"你好"
    那么 响应状态码为201

  场景: 列出消息
    假如 一个空白笔记本
    假如 一个空白会话
    假如 会话中有一条用户消息"第一条"
    当 请求当前会话的消息列表
    那么 响应状态码为200
    而且 响应列表至少包含1条记录

  场景: 消息分页
    假如 一个空白笔记本
    假如 一个空白会话
    假如 会话中有一条用户消息"消息一"
    假如 会话中有一条用户消息"消息二"
    假如 会话中有一条用户消息"消息三"
    当 使用分页参数limit=2请求消息列表
    那么 响应状态码为200
    而且 响应列表包含2条记录

  场景: 发送消息后会话更新时间变化
    假如 一个空白笔记本
    假如 一个空白会话
    当 发送一条用户消息"触发更新"
    那么 响应状态码为201
    当 请求当前笔记本的会话列表
    那么 响应状态码为200
