# language: zh-CN
功能: 模型管理
  作为一个知识工作者
  我希望能查看可用的 AI 模型
  以便为不同任务选择合适的模型

  场景: 列出所有模型
    假如 已启动应用
    当 请求模型列表
    那么 响应状态码为200
    而且 响应中"models"为列表

  场景: 按角色筛选模型
    假如 已启动应用
    当 按角色"chat"筛选模型
    那么 响应状态码为200
    而且 响应中"models"为列表
    而且 所有模型均包含角色"chat"

  场景: 获取指定模型详情
    假如 已知一个模型标识
    当 请求该模型详情
    那么 响应状态码为200
    而且 响应中"id"的值为该模型标识

  场景: 请求不存在的模型
    假如 已启动应用
    当 请求不存在的模型"nonexistent-model"
    那么 响应状态码为404
