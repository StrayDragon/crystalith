# language: zh-CN

功能: 提示词预设
  作为用户
  我希望能管理自定义提示词预设
  以便在问答中使用不同的提示策略

  背景:
    假如 已启动应用

  场景: 列出所有预设包含内置和自定义
    当 请求提示词预设列表
    那么 响应状态码为200
    而且 响应列表至少包含1条记录

  场景: 创建自定义预设
    当 创建提示词预设，触发词为"my-custom"，提示为"自定义系统提示"
    那么 响应状态码为201
    而且 响应中"trigger"的值为"my-custom"
    而且 响应中"source"的值为"custom"
    而且 响应中"enabled"为真

  场景: 触发词与内置预设冲突返回409
    当 创建提示词预设，触发词为"stats"，提示为"冲突测试"
    那么 响应状态码为409

  场景: 触发词重复返回409
    假如 已创建预设"dup-test"
    当 创建提示词预设，触发词为"dup-test"，提示为"重复测试"
    那么 响应状态码为409

  场景: 更新自定义预设
    假如 已创建预设"update-test"
    当 更新预设"update-test"的描述为"新描述"
    那么 响应状态码为200
    而且 响应中"description"的值为"新描述"

  场景: 删除自定义预设
    假如 已创建预设"delete-test"
    当 删除预设"delete-test"
    那么 响应状态码为204
