# language: zh-CN

功能: 知识问答
  作为用户
  我希望能向笔记本提问并获得回答
  以便从来源文档中获取信息

  背景:
    假如 已启动应用
    而且 一个名为"问答测试笔记本"的笔记本

  场景: 无来源时提问返回无证据回答
    假如 笔记本中无来源
    当 向笔记本提问 "什么是量子计算？"
    那么 响应状态码为200
    而且 响应中"evidence"为假
    而且 响应中"answer"包含"请先选择至少一个来源后再提问"
    而且 响应中"confidence"的值为0

  场景: 有来源时提问触发RAG流程
    假如 笔记本中有一篇来源"量子计算入门.md"
    当 向笔记本提问 "量子计算的核心原理是什么？"
    那么 响应状态码为200
    而且 响应中"evidence"为真
    而且 响应中"answer"包含"[1]"
    而且 响应列表至少包含1条记录

  场景: 指定source_ids过滤来源提问
    假如 笔记本中有多篇来源
    当 向笔记本提问 "文档内容" 并指定来源过滤
    那么 响应状态码为200

  场景: 使用stats预设提问
    假如 笔记本中有一篇来源"数据分析报告.md"
    当 向笔记本提问 "/prompt:stats 这份数据有什么特点？"
    那么 响应状态码为200
    而且 响应中"evidence"为真

  场景: 使用已禁用的预设提问失败
    假如 笔记本中有一篇来源"测试文档.md"
    当 向笔记本提问 "/prompt:disabled-test 某个问题"
    那么 响应状态码为400

  场景: 使用未知预设提问失败
    假如 笔记本中有一篇来源"测试文档.md"
    当 向笔记本提问 "/prompt:不存在的预设 某个问题"
    那么 响应状态码为400

  场景: 流式提问返回SSE事件
    假如 笔记本中有一篇来源"流式测试.md"
    当 流式提问 "请介绍一下流式响应"
    那么 响应状态码为200

  场景: 无来源时流式提问返回无证据SSE事件
    假如 笔记本中无来源
    当 流式提问 "什么是人工智能？"
    那么 响应状态码为200

  场景: 导出QA为Markdown格式
    假如 笔记本中有一篇来源"导出测试.md"
    而且 已完成一次问答
    当 导出问答记录为"markdown"格式
    那么 响应状态码为200

  场景: 导出QA为JSON格式
    假如 笔记本中有一篇来源"导出测试.md"
    而且 已完成一次问答
    当 导出问答记录为"json"格式
    那么 响应状态码为200
