# language: zh-CN
功能: 任务管理
  作为一个用户
  我希望能够查看和管理后台任务
  以便跟踪任务执行状态

  背景:
    假如 已启动应用
    并且 一个名为"任务测试笔记本"的笔记本

  场景: 获取任务状态
    假如 笔记本中有一个待处理任务
    当 发送 GET 请求"/v1/tasks/{任务[id]}"
    那么 响应状态码为200
    并且 响应中"type"的值为"refine"
    并且 响应中"status"的值为"pending"

  场景: 列出笔记本的任务
    假如 笔记本中有一个待处理任务
    当 发送 GET 请求"/v1/notebooks/{当前笔记本[id]}/tasks"
    那么 响应状态码为200
    并且 响应列表至少包含1条记录

  场景: 任务不存在时返回404
    当 发送 GET 请求"/v1/tasks/99999"
    那么 响应状态码为404

  场景: 取消不存在的任务返回404
    当 发送 POST 请求"/v1/tasks/99999/cancel"，内容为：
      """json
      {}
      """
    那么 响应状态码为404
