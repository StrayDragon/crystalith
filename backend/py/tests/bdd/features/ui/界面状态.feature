# language: zh-CN
功能: 界面状态管理
  作为一个用户
  我希望能够管理会话的界面状态
  以便在组件间共享和同步状态

  背景:
    假如 已启动应用
    并且 一个名为"界面测试笔记本"的笔记本
    并且 一个空白会话

  场景: 获取界面状态
    当 请求界面状态
    那么 响应状态码为200
    并且 响应中"session_id"的值为当前会话ID
    并且 响应中"shared_state_revision"的值为0

  场景: 处理界面事件
    当 发送界面事件，组件ID为"test-component"，事件名为"click"，基础版本为0
    那么 响应状态码为200
    并且 响应中"shared_state_revision"的值为1

  场景: 幂等事件处理
    假如 已发送一个界面事件
    当 使用相同的请求ID再次发送界面事件
    那么 响应状态码为200
    并且 响应中"shared_state_revision"未改变

  场景: 版本冲突返回409
    假如 已发送一个界面事件
    当 发送界面事件，组件ID为"test-component"，事件名为"update"，基础版本为0
    那么 响应状态码为409
