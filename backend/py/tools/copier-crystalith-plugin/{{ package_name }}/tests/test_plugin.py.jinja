from __future__ import annotations

from crystalith.shared.plugins.compliance import check_plugin

from {{ module_name }}.plugin import plugin


def test_plugin_is_compliant() -> None:
    issues = check_plugin("{{ plugin_id }}", plugin)
    assert issues == []
