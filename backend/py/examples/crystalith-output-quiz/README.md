# crystalith-output-quiz

Example `OutputTypePlugin` for Crystalith (`output_type = "QUIZ"`).

This package is intended as an in-repo reference implementation to validate the
`OutputTypePlugin` mechanism end-to-end.
