kind: llman.sdd.spec
name: "deep-research-ui"
purpose: "TBD - created by archiving change c77-deep-research-ui. Update purpose after archive."
valid_scope[2]: src/,tests/
requirements[38]{req_id,title,statement}:
  r400,Lab route replaces E1 Desk as deep entry,"深研产品入口 MUST 为 /research-lab/:nid（及工作区烧瓶导航）；Workspace 顶栏 E1 MUST NOT 再以 DeepResearchDesk 作为深研主表面；MUST NOT 恢复「暂不可用」壳或已删除 HITL agent 作为深研路径。"
  r401,H1 create form on desk,DeepResearchDesk MUST 提供显式 useNotebookSources 与 allowWeb（默认皆开）、台内 sourceIds 多选、以及浅/中/深 depth（默认中）；当 useNotebookSources 为 true 且未选来源、或两开关皆关、或 topic 为空时 MUST 禁用开始；sourceIds MUST NOT 隐式绑定工作区勾选状态。
  r402,Queue freshness without list SSE,Desk 队列 MUST 在打开时拉取 notebook 作用域 listRuns；当存在非终态 Run 时 MUST 以短间隔轮询刷新，全终态或关闭 Desk 时 MUST 停止轮询；创建成功、关闭详情返回、以及详情内导致状态变化的操作之后 MUST 立即再拉 list；MUST NOT 在本变更引入 notebook 级 list SSE。
  r403,Run detail layer keeps E1 mounted,打开 Run 详情时 MUST 使用高于 E1 的 modal Layer，且 E1 Desk MUST 保持挂载；关闭详情后 MUST 恢复 E1 的 tab/查询/队列原样；Escape 或点击详情遮罩 MUST 仅关闭详情；同时 MUST 最多展示一个 Run 详情（切换卡片替换当前详情）。
  r404,"Status-driven primary surface",Run 详情 MUST 按状态切换主表面：queued/running/awaiting_confirm 以研究图为主且报告草稿默认折叠；completed 以结构化报告为主且图降为只读次级入口；failed/cancelled 以状态说明为主并在可用时展示末次图或草稿；MUST NOT 提供并列的独立「研究思路」页面，也 MUST NOT 使用永久左右分栏作为唯一布局。
  r405,ResearchGraph applies D1 patches,详情内研究图 MUST 由新建 ResearchGraph（xyflow）渲染，并 MUST 应用 SSE graph_patch 增量 upsert/remove；节点 MUST 反映 conclusionStatus 语义色（工作区语义色；pruned 降透明度）；MUST NOT 复用或改造 MindmapViewer 作为深研图实现。
  r406,Node inspector prune fork convert,在 running 或 awaiting_confirm 时，用户单击节点 MUST 打开轻量 inspector，并 MUST 提供剪枝与 fork（fork 可带可选 hint）且在执行前 MUST 有轻量二次确认；节点 Convert 入口 MUST 淡化展示；completed/failed/cancelled 时图与 inspector MUST 只读且 MUST NOT 继续 prune/fork。
  r407,M1 confirm bar on detail,当 Run 进入 awaiting_confirm 时，详情顶栏 MUST 展示 M1 确认条（预算将尽或扩支路），并 MUST 提供与 c76 对齐的确认动作；图上对应节点/区域 MUST 可高亮；MUST NOT 恢复逐步审批计划 UI。
  r408,CitationsControl adapter for report,报告面 MUST 复用 CitationsControl 展示引用；前端 MUST 将 Run 全局 citation map 适配为 CitationsControl 所需 Citation 形；MUST NOT 为深研另建平行的引用弹层组件（除非复用组件无法满足最小字段再做最小扩展）。
  r409,Convert feedback is toast only,显式 convertToNote/convertToSource 成功或失败 MUST 以 toast（或同等非模态反馈）通知用户；MUST NOT 弹出强制转化确认对话框；成功反馈 MAY 提供打开笔记或来源的弱链接。
  r410,Research error surfacing,Desk 与详情 MUST 将 RESEARCH_INVALID_STATE 与 RESEARCH_BUDGET 映射为用户可见内联错误与 toast；其他错误 MUST 走既有 AppHttpError/信封展示路径；MUST NOT 静默吞掉这些专用错误码。
  r411,"Retire Desk and legacy deep-search dual track","本变更 MUST 切断顶栏 deep Tab / DeepResearchDesk 与来源栏 Deep Research 模式残留（含死组件 SourcesPanelSearchSection 与 Deep SearchMode）；深研唯一主入口 MUST 为 /research-lab；来源栏 MUST NOT 再嵌深研任务队列或 Fast/Deep 模式切换作为主路径；MUST NOT 删除 ResearchRun 后端 API。"
  r412,No run URL deep link in v1,首发 Deep Research UI MUST NOT 依赖 URL query/path 深链打开特定 Run；打开详情 MUST 由会话内 UI 状态驱动。
  r413,i18n and quality gates after Desk removal,深研相关用户可见文案 MUST 走前端 i18n（t()）；本变更 MUST 提供/更新 Vitest 证明顶栏无 deep Desk，并至少保留一条可达 Lab 入口的回归（testid 或等价）；MUST 在不动 ResearchRun 路由的前提下通过 typecheck 与 server research 测试子集。
  r414,Pruned branch fade and failed merge retain,研究图 UI MUST 将 conclusionStatus=pruned 的节点以降透明度（或等价弱化）展示；触达 pruned 节点的边 MUST 弱化描边但仍可见；来自 pruned 节点的 merge 边 MUST 保留且标签可继续显示「汇入」（或产品等价文案），MUST NOT 因剪枝从画布移除该 merge；当存在失败汇入时，结论节点 MUST 以可见文案或脚注提示「部分汇入失败」（或等价）。
  r415,Thin client command ports for Lab,Deep Research Lab 展示层 MUST 以 GET ResearchRun 与 SSE graph_patch/status/confirm 作为图与状态的展示权威；用户剪枝、分叉、确认、取消、转化、节点字段修改 MUST 仅调用对应 HTTP 命令口；客户端 MAY 做可丢弃的本地预览，但在命令响应或后续 graph_patch 到达后 MUST 以服务端结果为准回写；默认生产路径 MUST NOT 将本地定时回放或伪造图作为 Run 的权威状态机（fixture 仅显式非生产开关）。
  r416,"Role-preferring graph render",研究图 UI MUST 优先依据节点 role（question|research|conclusion）区分交互与保护态展示；当 role 缺失时 MUST 回退到既有 id 前缀或 merge 目标启发式以保持兼容；MUST NOT 因缺少精致视觉规格而阻塞命令口接线。
  r417,Node chat UI wires proposals to commands,节点对话 UI MUST 消费独立 chat SSE 展示正文与提案；用户接受剪枝/分叉/改查询/定态/确认类提案时 MUST 调用对应 prune/fork/PATCH/confirm 命令口；MUST NOT 仅在本地改图作为权威；打开报告类动作可为纯前端导航。
  r418,Desk canvas prefs from Lab mechanisms,Desk 研究图 MUST 提供与 Lab 对齐的画布机制入口（至少布局方向或算法偏好、以及小地图可开关之一档）；这些偏好 MAY 仅存于客户端；MUST NOT 因此新增服务端编排 API 或改变图 SSOT。
  r419,Revisions UI over server snapshots,Desk（或报告面）MUST 能列出并查看服务端 revisions，并 MUST 支持创建当前快照与恢复到某 revision（经 API）；MUST NOT 继续以仅 sessionStorage 的 Lab CoW 作为生产权威版本源。
  r420,"Lab is deep-research primary surface","/research-lab/:nid MUST 作为深研产品主表面；工作区 MUST 经烧瓶（或等价）导航进入；在 c82 接线前 Lab 会话权威 MAY 为 fixture，但 MUST 可走通既定设计面；MUST NOT 将顶栏搜索面板恢复为深研主入口。"
  r421,"xlsx-lib fixture drives Lab walkthrough","Lab 主路径 MUST 默认使用写死研究任务 scenario id=xlsx-lib；该 fixture MUST 足以演练图交互、节点抽屉与提案、报告与 cite、revisions 与画布设置等既有 Lab 设计面；MUST NOT 在本变更引入第二套并行正式 scenario 作为主路径。"
  r422,Inventory Lab versus ResearchRun,本变更 MUST 产出 inventory 文档，逐项对照 Lab 设计面与现有 ResearchRun HTTP/SSE 合约（Keep/Gap/Extra/Ambiguous）；MUST NOT 在本变更将 fixture 替换为 Eden 权威态（留待 c82）。
  r423,Lab wires via Eden and shared schemas,"Lab 对接 ResearchRun MUST 使用 Eden treaty 与 @crystalith/shared research schemas（及 ErrorEnvelope）；MUST NOT 新增与 shared 同域异形的平行 wire DTO；MUST NOT 删除或绕过 /v2/notebooks/:nid/research*。"
  r424,"xlsx-lib fixture is non-authoritative by default","在默认生产配置下，xlsx-lib fixture MUST NOT 作为研究过程权威；用户在 Lab 主路径执行的图变更 MUST 反映到服务端 Run（经 c81 合约）；fixture 模式若保留 MUST 需显式开关且不可被误认为已接线生产态。"
  r425,Lab task inbox stays fresh with Run state,"Lab 任务抽屉与 ResearchTasksTrigger badge MUST 共享 notebook 作用域 listRuns（SWR key research-tasks）；Compose 创建成功、HTTP confirm/cancel/prune/fork 成功、以及 SSE status/confirm/report_ready 之后 MUST 立即 mutate 或等效 refresh 任务列表；activeCount MUST 仅统计 status 为 queued、running 或 awaiting_confirm 的任务，completed/failed/cancelled MUST NOT 计入；打开任务抽屉时 MAY revalidate；切换 notebook MUST 丢弃旧 notebook 的 SWR 缓存。"
  r426,Lab exposes cancel for active Eden runs,"Eden 模式下当 ResearchRun status 为 queued、running 或 awaiting_confirm 时，Lab MUST 暴露取消入口（顶栏主/次操作和/或任务抽屉行操作）；用户触发后 MUST 调用 POST …/research/:rid/cancel；成功或 SSE 终态后 Run status MUST 变为 cancelled，且 MUST 按 r425 立即 refresh 任务 inbox；completed、failed、cancelled MUST NOT 再提供取消。"
  r427,Eden primary action maps to cancel not fixture pause,Eden 模式下顶栏主操作 MUST 与 ResearchRun 生命周期对齐：queued 或 running 时主操作 MUST 映射协作 cancel（MUST NOT 调用 fixture pause 空操作）；awaiting_confirm 时 MUST 保持「生成结论」主操作与「继续深挖」次操作；cancel 成功后 UI MUST 展示 cancelled 终态说明。
  r428,Compose copy switches by eden or fixture mode,LabComposePanel MUST 按 mode（eden 或 fixture）切换说明文案与底部 hint；eden 模式 MUST NOT 将 xlsx fixture、演示回放或「接线后将创建」表述为权威或默认路径；fixture 模式 MAY 保留 fixture 演示说明。
  r429,"Eden example topic avoids fixture-only wording","Eden 模式下「填入示例主题」控件 MUST 使用中性文案或隐藏 fixture 专属措辞（如 xlsx 选型）；fixture 模式 MAY 保留 xlsx-lib 示例主题按钮与文案。"
  r430,Eden openReport navigates to real report page,"Eden 模式下 Lab MUST NOT 以 toast 代替报告页；用户触发 openReport（顶栏、抽屉或等价 CTA）时 MUST 导航至 /research-lab/:nid/report 并携带当前 Run 的 ?rid= query；MUST NOT 调用 fixture ensureDefaultRevision 作为 Eden 默认路径。"
  r431,LabReportPage loads Run report as SSOT,"Eden 模式下的 LabReportPage MUST 通过 GET …/research/:rid 加载 ResearchRun.report 作为报告正文与 citations 的权威源；MUST 渲染结构化 sections；MUST NOT 以 labRevisions 或 sessionStorage 作为 Eden 默认权威。"
  r432,Eden report uses CitationsControl adapter,Eden 报告面 MUST 将 ResearchReport.citations 经 adapter 复用 CitationsControl 展示引用（对齐 r408）；MUST NOT 为 Eden 报告另建平行引用弹层。
  r433,Eden export reads Run report not stub toast,Eden 模式下 exportSuggestedFromGraph（或等价「从图导出建议报告」）MUST 从当前 Run 的 report 或等价结构化数据生成导出；MUST NOT toast「Eden 报告导出待接」；无 report 时 MUST 展示可见错误。
  r434,Eden node drawer shows Run evidence citations,Eden 模式下 LabNodeDrawer 的引用列表 MUST 由当前 ResearchRun 上节点 evidenceIds 映射而来（标题与 snippet 至少其一）；当 evidenceIds 非空且 Run 已返回对应证据时 MUST NOT 显示「暂无引用」；MUST NOT 以 fixture scenario.citations 作为 Eden 默认引用源。
  r435,Terminal Run node labels reflect server state,当 ResearchRun status 为 completed、failed 或 cancelled 时，研究图与节点抽屉中的节点状态文案 MUST 反映服务端终态；结论节点在 completed 后 MUST NOT 长期显示「排队…」或等价进行中文案。
  r436,SSE and terminal GET refresh align node fields,Eden Lab 在收到 SSE graph_patch 后 MUST 合并节点与边；在 status 进入 completed、failed 或 cancelled 以及 report_ready 后 MUST getResearchRun 或等效全量刷新以对齐 node.phase、conclusionStatus 与 evidenceIds。
  r437,awaiting_confirm progress does not contradict playing,当 Run status 为 awaiting_confirm 时，Lab MUST NOT 同时展示「多源探索」playing 横幅与「已暂停」类矛盾文案；playing 推导 MUST 仅对 queued 或 running 为 true；进度条 phase MUST 为 awaiting_confirm。
scenarios[52]{req_id,id,given,when,then,feature}:
  r400,"lab-route-is-deep-entry",用户要从工作区进入深研,打开深研,"进入 /research-lab/:nid 而非顶栏 DeepResearchDesk",false
  r401,"empty-sources-disables-start",用来源开关开启且台内未选任何来源,用户尝试开始深研,开始控件保持禁用且不得发出创建请求,false
  r402,"polling-stops-when-terminal",Desk 打开且队列仅含 completed/failed/cancelled,等待超过一个轮询间隔,客户端不再为 list 发起定时轮询请求,false
  r403,"escape-closes-detail-keeps-desk",E1 deep tab 与某 Run 详情同时打开,用户按下 Escape,详情关闭且 E1 Desk 仍保持打开与原 tab,false
  r404,"completed-shows-report-primary",Run 状态为 completed 且已有报告,用户查看详情主表面,结构化报告为主内容且图不作为唯一主画布,false
  r405,"graph-patch-updates-canvas",详情已订阅 SSE 且图已渲染,收到含新节点的 graph_patch,ResearchGraph 上出现对应节点且不经过 MindmapViewer,false
  r406,"prune-requires-light-confirm",Run 为 running 且某节点可选中,用户在 inspector 触发剪枝,须先通过轻量二次确认才会调用 prune API,false
  r407,"m1-bar-on-awaiting-confirm",Run 进入 awaiting_confirm,用户查看详情,顶栏出现 M1 确认条且可执行继续或出报告等动作,false
  r408,"report-uses-citations-control",终局报告含 citation map,用户打开引用入口,复用 CitationsControl 弹层展示适配后的引用,false
  r409,"convert-success-toast",用户从节点或报告触发 convertToNote 且服务成功,观察反馈,出现成功 toast 且无强制转化对话框,false
  r410,"budget-error-visible",API 返回 RESEARCH_BUDGET,用户在 Desk 或详情中操作,界面展示可见错误提示而非静默失败,false
  r411,"sources-has-no-deep-mode-toggle",用户打开来源面板,查看搜索/模式 UI,不见 Fast/Deep 深研模式切换与深研队列主入口,false
  r412,"no-query-deep-link-required",用户仅通过 Desk 卡片打开详情,不修改 URL,详情仍可打开且不依赖 researchRun query,false
  r413,"topbar-fast-only-and-lab-reachable",e2e 或 Vitest 进入工作区,打开顶栏搜索并检查深研入口,顶栏无 deep Desk；Lab 入口仍可发现,false
  r414,"prune-keeps-failed-merge-on-canvas",详情图已渲染且某研究节点有 merge 到结论,用户确认剪枝该节点后 graph_patch 到达,该节点呈 pruned 弱化态，其 merge 边仍可见且弱化，结论侧出现部分汇入失败提示,true
  r415,"lab-prune-calls-http",Lab 已订阅 Run SSE 且用户确认剪枝,执行剪枝,发出 POST prune 并以返回或 graph_patch 更新画布,true
  r416,"role-or-prefix-protects-sink",图中 conclusion 带 role=conclusion（或 node_conclusion_ 前缀）,用户查看剪枝入口,结论节点不可作为可剪枝研究支路入口（与服务端保护一致）,true
  r417,"accept-prune-proposal-calls-api",节点 chat 展示 pending prune 提案,用户点击接受,客户端调用 POST prune（而非只改本地节点状态）,true
  r418,"canvas-pref-does-not-call-graph-api",用户切换画布布局方向或小地图,偏好生效,未因此发出修改 research graph 的命令口请求,true
  r419,"restore-revision-refreshes-graph",用户选择恢复某 revision,恢复成功,画布与报告反映该快照内容（经 GET 或 graph_patch）,true
  r420,"open-lab-as-primary",用户从工作区进入深研,导航完成,"位于 /research-lab/:nid 主表面",false
  r421,"default-scenario-xlsx-lib",用户打开 Lab 主路径,查看当前 scenario,"为 xlsx-lib 且可演练主要设计面",false
  r422,"inventory-lists-gaps",c80 完成,查阅 inventory.md,含 Lab↔ResearchRun 对照表且标出 Gap,false
  r423,"lab-uses-eden-not-parallel-dto",Lab 创建或刷新 Run,查看客户端实现,经 Eden + shared 类型，无平行 research DTO 文件,false
  r424,"default-path-not-fixture-authority",默认配置打开 Lab 主路径并开始研究,推进图状态,"权威态来自 ResearchRun 而非 xlsx-lib timer",false
  r425,"compose-create-increments-badge",Lab 已接线 Eden 且当前 activeCount 为 0,用户 Compose 创建 Run 成功,任务列表含新 Run 且 badge activeCount 增加,true
  r425,"run-terminal-clears-badge",列表有一项 awaiting_confirm 且 badge 为 1,用户 confirm 完成或 SSE 将 Run 变为 completed,列表 status 更新且 badge 归零,true
  r425,"sse-status-refreshes-inbox",已订阅 Run SSE 且任务 inbox 已展示,收到 status 或 confirm 事件,任务列表经 refresh 反映最新 status,true
  r426,"cancel-from-header-running",Eden Lab 已打开且 Run status 为 running,用户点顶栏取消并确认,POST cancel 成功且 Run 变为 cancelled,true
  r426,"cancel-from-drawer-awaiting-confirm",任务抽屉有一项 awaiting_confirm,用户从抽屉触发取消,cancel 成功且列表 status 为 cancelled,true
  r426,"cancel-refreshes-inbox",c84 inbox refresh 已接线,Eden cancel 成功,任务列表经 refresh 反映 cancelled 且 badge 不计入进行中,true
  r427,"running-primary-is-cancel",Eden Run 为 running 且顶栏展示主操作,用户查看主按钮标签与行为,映射 cancel 而非空 pause,true
  r427,"awaiting-confirm-keeps-finish-dig",Eden Run 为 awaiting_confirm,用户查看顶栏,主操作为生成结论且次操作为继续深挖,true
  r428,"eden-compose-no-fixture-authority",Lab Compose 以 eden 模式打开,用户阅读说明与 hint,文案不提 xlsx fixture 或演示回放为权威,true
  r428,"fixture-compose-keeps-demo-copy",Lab Compose 以 fixture 模式打开,用户阅读说明,MAY 保留 fixture 演示回放说明,true
  r429,"eden-example-topic-neutral",Lab Compose 为 eden 模式,用户查看示例主题按钮,文案为中性且无 xlsx 选型专属措辞,true
  r430,"eden-open-report-no-stub-toast",Eden Lab 已加载 completed Run 且 report 存在,用户点「打开独立报告页」,"导航至 /research-lab/:nid/report?rid= 且无 stub toast",true
  r430,"completed-cta-opens-report",Run status 为 completed 且 reportVisible,用户点顶栏查看报告,进入 Eden 报告页而非 toast,true
  r431,"report-page-loads-run-report",报告页以 eden 模式打开且 rid 有效,页面加载完成,展示 GET run 返回的 report 标题与章节,true
  r431,"eden-report-not-sessionstorage",Eden 报告页打开,sessionStorage 无 labRevisions,仍以 Run.report 渲染正文,true
  r431,"report-rid-query-required",用户从 graph 页 ?rid= 打开报告,导航完成,报告页读取同一 rid 的 Run,true
  r432,"eden-citations-control",Eden 报告含 citation map,用户打开引用入口,CitationsControl 展示适配后的引用,true
  r433,"eden-export-from-report",Eden Run 已有 report,用户触发从图导出,生成 markdown 且无「待接」toast,true
  r433,"eden-export-no-report-error",Eden Run 无 report,用户触发导出,可见错误提示且无 fixture revision 写入,true
  r434,"drawer-shows-evidence-when-ids-present",Eden Run completed 且某节点 evidenceIds 非空,用户打开该节点抽屉,引用列表展示至少一条证据摘要,true
  r434,"eden-not-scenario-citations",Eden Lab 已加载 Run,查看 LabNodeDrawer citations 来源,来自 Run 证据映射而非 scenario.citations,true
  r435,"conclusion-not-queued-after-complete",Run 已 completed,用户查看结论节点卡片,非「排队…」终态文案,true
  r435,"failed-run-readonly-labels",Run status 为 failed,用户查看图节点,展示失败/终态语义,true
  r436,"terminal-status-triggers-full-refresh",SSE 将 Run 变为 completed,客户端处理事件,经 GET run 对齐节点 phase 与 evidenceIds,true
  r436,"graph-patch-merges-evidence-ids",SSE graph_patch 更新节点 evidenceIds,合并后打开抽屉,引用列表反映新 ids,true
  r437,"awaiting-confirm-no-playing-banner",Run 为 awaiting_confirm,用户查看作业台横幅,示等待确认且无探索 playing 横幅,true
  r437,"running-shows-playing",Run 为 running,用户查看 playing 状态,playing 为 true 且无「已暂停」横幅,true
