kind: llman.sdd.spec
name: "delivery-and-deployment"
purpose: 定义部署与交付最小闭环：部署目录约束、生产 compose 入口、离线 profile、镜像构建基线与文档可发现性。
valid_scope[2]: src/,tests/
requirements[15]{req_id,title,statement}:
  r1,Deployments directory is canonical,仓库 MUST 以 `deployments/` 作为部署清单根目录，并包含 `deployments/prod/`。
  r2,Production stack is runnable from layered prod manifests,"`deployments/prod` MUST 提供可一键启动的生产栈配置，且该配置 MUST 将 `frontend` 与 `backend` 定义为核心必选服务；任何附加服务 MUST 通过 profile 或等价组合开关显式启用。 规范术语与 Compose 服务名映射 MUST 固定为：`frontend -> web`，`backend -> api`。"
  r3,Stateful services persist data,有状态服务 MUST 使用 volume 持久化，容器重建后数据不丢失。
  r4,"Runtime images are optimized and non-root",生产镜像 MUST 使用多阶段构建且以非 root 用户运行。
  r5,Versioned runtime images are published to GHCR,"系统 MUST 将生产运行镜像以版本化方式发布到 GHCR（或等价 registry），并覆盖 `api` 与 `web` 两个核心服务。 镜像标签 SHOULD 支持 SemVer（`X.Y.Z`）与可追溯 SHA 标签（`sha-<shortsha>`），并包含 OCI labels（source/revision/version）。"
  r6,Optional offline Ollama composition is supported,生产 compose MUST 支持可选本地 Ollama 组合（overlay 或 `ollama` profile），且在未启用该可选组合时 MUST 允许使用外部 Ollama 或其他 embedding/chat 提供方，不得强制绑定本地 Ollama 容器。
  r7,Deployment entrypoints are documented,`deployments/README.md` MUST 清晰区分核心最小拓扑与可选增强拓扑，并为每个可选 profile 提供启用条件、依赖说明与验收步骤。
  r8,"Runtime config is documented as YAML-first","部署文档 MUST 将 `config/app.yaml`（模板渲染 + overlays + schema 校验）作为运行时业务配置的权威来源，并明确 secrets 位于 `config/secret.env`。 文档 MAY 提及 `.env` 作为两类用途的承载： - 部署/构建静态参数（端口、镜像、构建镜像源等） - 非 secret 的模板输入（供 `{{ env.* }}` 使用） 但文档 MUST 明确：密钥 MUST NOT 放入 `.env`，而 MUST 放入 `config/secret.env`。"
  r9,.env.example contains only deploy/build static parameters,`.env.example` MUST 仅包含部署/构建静态参数模板，以及可选的**非 secret**模板输入示例；并 MUST NOT 包含任何 secret（例如 `OPENAI_API_KEY`、数据库密码等）。
  r10,Overlays do not inject business config into api env,生产 compose overlays MUST 仅负责启动可选子服务与其持久化/网络拓扑，并 MUST NOT 通过 `api.environment` 注入业务运行配置。
  r11,SDK docs use correct deployment entrypoints for base_url,文档中的 SDK 示例 MUST 使用与实际部署拓扑一致的 `base_url`/`baseUrl` 推荐值，并避免引用可选依赖服务端口（例如 Chroma 的 `8000`）作为 API 入口。
  r12,"Self-host diagnostics are discoverable",自托管部署 MUST 提供可发现的诊断入口与最小排障路径，以降低用户定位成本。
  r13,Backup and restore guidance exists,部署文档 MUST 描述数据备份与恢复的最小闭环（含风险与验证步骤）。
  r14,Local development entrypoint mirrors composition model,本地开发入口 MUST 与部署入口共享“核心 + 可选 profile”模型，使开发与生产在依赖启停语义上保持一致。
  r15,"Compose composition follows merge-first rule","部署组合 MUST 以 `-f` merge（core + optional overlays）作为主路径；`profiles` 可作为可选增强，`include` 仅可用于强隔离模块，且不得引入同名服务/网络/卷冲突。"
scenarios[17]{req_id,id,given,when,then}:
  r1,"locate-production-manifests","",新操作者在仓库中寻找生产部署入口,系统 SHALL 在 `deployments/` 下提供清晰的清单与 `deployments/prod/` 目录
  r2,"deploying-core-production-stack","",运维使用生产清单在未启用可选 profile 的情况下部署,系统 SHALL 启动前后端核心服务并保持可选服务默认关闭
  r3,"recreate-containers-without-data-loss","",有状态服务容器被重建或升级,系统 SHALL 通过 volume 保持数据不丢失
  r4,"runtime-container-does-not-run-as-root","",运维检查生产容器运行用户,生产镜像 SHALL 以非 root 用户运行并保持最小运行时体积
  r5,"release-tag-publishes-both-images","",维护者推送 `vX.Y.Z` tag,系统 SHALL 将 `api` 与 `web` 镜像推送到 GHCR
  r5,"published-images-pass-minimal-runtime-health-checks","",用户从 GHCR 拉取同一版本的 `api` 与 `web` 镜像并启动最小拓扑,`api` SHALL 通过 `GET /health` 健康检查
  r6,"using-external-ollama-without-profile","",用户未启用本地 Ollama 可选组合且配置了外部 Ollama 地址,系统 SHALL 使用外部地址完成模型调用并保持部署栈有效
  r7,"new-operator-follows-deployment-docs","",新操作者仅按部署文档执行核心启动步骤,系统 SHALL 在不引入额外服务的情况下可运行并可通过基础健康检查
  r8,"new-operator-configures-runtime-without-business-env","",新操作者按部署文档启动核心栈,文档 SHALL 指引其通过编辑 `config/app.yaml` 与 `config/secret.env` 完成业务配置
  r9,"copying-env-example-does-not-imply-runtime-configuration","",用户复制 `.env.example` 为 `.env` 且仅修改端口/镜像等静态参数,系统 SHALL 仍以 `config/app.yaml` 作为运行时配置来源
  r10,"enabling-storage-overlay-does-not-require-env-wiring","",用户在核心清单上叠加 storage overlay 启动,`api` SHALL 无需额外业务 env 注入即可运行
  r11,"local-dev-base-url-matches-backend-dev-port","",用户按本仓库本地开发路径启动后端（`just dev` 等价入口）,"SDK 文档 SHALL 推荐 `http://127.0.0.1:8032`（或等价的本地后端实际端口）作为默认 `base_url`"
  r11,"compose-base-url-uses-the-web-front-door","",用户按生产式 Docker Compose 启动核心栈（`web` + `api`）,"SDK 文档 SHALL 推荐使用 `http://localhost:${CL_WEB_PORT:-8080}`（与 Web UI 同入口）作为 `base_url`"
  r12,"operator-can-find-diagnostics-quickly","",用户在自托管环境遇到“无法连接/功能不可用”,系统 SHALL 提供明确的诊断入口（UI 或文档）
  r13,"user-performs-minimal-backup","",用户希望备份并迁移其本地数据,文档 SHALL 明确最小备份集与恢复步骤
  r14,"local-dev-with-optional-profile","",开发者在本地启用可选 profile,本地运行形态 SHALL 与部署形态在服务组合与探活行为上保持一致
  r15,"enabling-optional-overlays-with-merge","",用户基于核心清单叠加一个或多个可选 overlay,系统 SHALL 仅启用被显式选择的可选服务，且核心服务定义保持可预期
