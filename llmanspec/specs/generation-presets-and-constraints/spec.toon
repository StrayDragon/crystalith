kind: llman.sdd.spec
name: "generation-presets-and-constraints"
purpose: 为不同生成类型提供可理解、可复用的预设（presets）与显式约束覆盖（constraints/knobs），使“能生成”升级为“能按意图生成”，并与类型契约保持一致。
valid_scope[2]: src/,tests/
requirements[8]{req_id,title,statement}:
  r33,生成类型必须支持显式预设选择,系统 MUST 允许每种生成类型声明并选择显式预设，以帮助用户稳定启动高价值生成路径。
  r91,生成请求必须支持显式约束覆盖,系统 MUST 允许用户在允许范围内覆盖生成约束，而不是只能依赖隐式默认值。
  r128,预设与约束不得反向定义生成类型,系统 MUST 保持预设与约束处于“可控生成”层，而不是重写生成类型本身。
  r164,预设必须绑定到生成类型（generationTypeId）,"系统 MUST 使用 `typed-generation-framework` 的公共词汇把预设绑定到某个生成类型，避免出现跨类型的隐式耦合。"
  r199,结果必须回传本次生效的控制配置（effective controls）,系统 MUST 在生成结果中回传本次生效的预设与控制项配置，以支持复用与“保存为预设”。
  "qa-stats-preset-must-exist",QA MUST support the stats preset with chart/table structured output,"QA MUST 支持 stats preset（与 v1 presets.py:93-99 一致）：包含 STATS_SYSTEM_PROMPT、StatsChart/StatsTable 校验、专用生成 + JSON 解析路径（v1 api.py:289-304 与 457-504）。实现：presets.ts + handler.ts（c48）；解析失败 SHALL 回退默认文本 QA。session sharedState.ui chart/table mounts 为后续（前端卡片已 orphan-remove）"
  "qa-contextstats-must-use-real-tokens",QA ContextStats MUST use real token counts and compression flag,"QA 的 ContextStats 字段（totalTokens/systemTokens/historyTokens/retrievalTokens/queryTokens/maxTokens/compressed）MUST 使用真实 token 计数；compressed 标志 SHALL 在历史被截断/摘要时为 true"
  "qa-export-must-match-v1-citation-and-sources-shape",QA export MUST match citation line format and sources meta shape,"QA export（markdown/json）MUST：(1) markdown citation 行 SHALL 包含 pageNumber/paragraphIndex；(2) JSON sources meta SHALL 为 sourceId/sourceName/mimeType/parserType 且 notebook-scoped；(3) JSON SHALL 含顶层 notebookId。markdown SHALL 含 Notebook ID 行"
scenarios[8]{req_id,id,given,when,then,feature}:
  r33,用户选择某类生成的预设,"",用户准备发起某个生成类型,系统 SHALL 展示该类型可用的预设,true
  r91,用户调整生成约束,"",用户选择某个预设后继续调整长度或证据要求,系统 SHALL 接收结构化约束覆盖,true
  r128,新增某个生成类型的预设,"",系统为一种已有生成类型新增预设,该变更 SHALL 建立在既有类型契约之上,true
  r164,系统加载某个预设,"",系统加载或创建某个预设,该预设 SHALL 显式声明其绑定的 `generationTypeId`,true
  r199,用户希望复用一次效果较好的生成,"",用户查看某次生成结果并希望复用其配置,系统 SHALL 回传该次生效的 `presetId` 与控制项值集合,true
  "qa-stats-preset-must-exist","stats-preset-request",客户端请求 preset=stats,系统执行 QA 生成,"系统 SHALL 使用 STATS_SYSTEM_PROMPT 并产出 StatsChart/StatsTable 结构化 JSON（与 v1 api.py:457-504 一致）",true
  "qa-contextstats-must-use-real-tokens","contextstats-compression",历史消息累积超过 maxTokens,系统构造 QA 上下文,ContextStats.compressed SHALL 为 true（如 v1 触发历史压缩）；token 数值 SHALL 来自真实 token 计数,true
  "qa-export-must-match-v1-citation-and-sources-shape","export-markdown-citations",用户导出 QA 会话为 markdown,系统生成 export,"citation 行 SHALL 含 page/para 信息；JSON sources SHALL 含 mimeType/parserType 且 notebook-scoped",true
