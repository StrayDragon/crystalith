kind: llman.sdd.spec
name: "service-composition-profiles"
purpose: 定义核心服务与可选子服务的组合规则、可观测状态与降级语义，确保默认最小拓扑可运行且可逐步增强。该规范用于统一“核心必选 + 可选增强”的组合心智模型，避免不同环境下的启停语义漂移。
valid_scope[2]: src/,tests/
requirements[7]{req_id,title,statement}:
  r1,Runtime topology has two mandatory core services,系统 MUST 将 `frontend` 与 `backend` 作为唯一必需核心服务；其他运行时能力 MUST 以可选子服务形式声明，不得阻塞核心服务启动。
  r2,Optional subservices are composable by overlay/profile,系统 MUST 支持通过 compose overlay（或 profile）显式启用可选子服务（例如向量库、搜索引擎、离线模型服务），并允许改为外部托管地址接入。
  r3,Optional subservice contract includes health and degrade policy,每个可选子服务 MUST 定义连接配置、探活方式、失败提示与降级策略；当可选子服务不可用时，系统 MUST 返回明确的可恢复错误而非导致核心服务不可用。
  r4,Composition state is observable for operators,系统 MUST 暴露当前组合状态（启用的 profile、可选服务健康状态、最近探活时间）以支持本地排障和部署验收。
  r5,Optional composition does not depend on env injection,当用户通过 compose overlay/profile 启用可选子服务时，系统 MUST 不依赖向核心服务（例如 `api`）注入业务 env 才能完成装配；核心服务 MUST 通过 `config/app.yaml` 的配置与端点选择机制连接可选子服务。
  r6,Composition diagnostics show selected endpoints,"系统 MUST 在组合诊断输出（例如 `/health/dependencies`）中展示： - 可选子服务是否启用 - 探活状态与恢复提示 - 实际选用的连接端点（若已锁定）"
  r7,Compose source manifests are authoritative and reviewable,系统 MUST 以分层 compose 源文件作为权威配置来源，组合结果 MUST 可通过标准 compose 展开与审查流程验证，避免隐藏式模板逻辑。
scenarios[7]{req_id,id,given,when,then}:
  r1,"core-only-startup","",用户使用默认核心配置启动系统且未启用任何可选子服务,前端与后端 SHALL 成功启动并提供核心研究工作流能力
  r2,"enabling-optional-composition","",用户启用某个可选 overlay/profile,系统 SHALL 仅装配该组合声明的子服务与其配置，并保持未启用组合不生效
  r3,"optional-service-unavailable","",可选子服务探活失败,系统 SHALL 记录依赖不可用状态并对相关增强功能返回清晰错误，同时保持核心路由可用
  r4,"composition-diagnostics-queried","",运维或开发者查询运行时状态,系统 SHALL 返回核心服务状态以及各可选子服务的健康细节
  r5,"enable-optional-overlay-without-api-env-wiring","",用户启用某个可选 overlay/profile（例如 redis/storage/searxng/ollama）,系统 SHALL 在不需要额外业务 env 注入的情况下完成连接或降级
  r6,"operator-inspects-optional-dependency-status","",运维查询 `/health/dependencies`,输出 SHALL 包含可选依赖的 enabled/status/endpoint（或未锁定原因）与 recovery_hint
  r7,"review-composed-deployment-topology","",用户或评审者检查核心清单与可选 overlay 的组合结果,系统 SHALL 提供可追溯的服务来源与明确的 profile 启用边界
