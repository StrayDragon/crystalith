kind: llman.sdd.spec
name: "docs-site"
purpose: "定义仓库内 docs-site 的布局与组织规则：使用 Zensical（MkDocs-compatible 配置格式）构建站点，以 `docs/doc/` 作为唯一文档真源，并将站点构建产物收敛到 `docs/site/`。"
valid_scope[2]: src/,tests/
requirements[5]{req_id,title,statement}:
  r1,Documentation site exists,"系统 MUST 在 `docs/zensical.toml` 提供 Zensical 的 MkDocs-compatible 配置，并允许从仓库 Markdown 源构建文档站点。"
  r2,Canonical docs live under docs/doc,"系统 MUST 将 `docs/doc/` 视为 docs-site 的唯一文档真源（curated manuals + controlled reference）。"
  r3,Site build output is scoped under docs/site,"系统 MUST 将 docs-site 的构建产物输出到 `docs/site/`，避免污染仓库根目录。"
  r4,Site scope stays curated,"系统 MUST 将 docs-site 范围限制为 `docs/doc/` 下的 curated 页面与仓库脚本生成的受控 reference 页面，并且 MUST NOT 将 `openspec/specs/**`、`openspec/changes/**`（含 archive）或其它不受控目录作为站点页面来源。"
  r5,Legacy scattered docs are moved and removed after reorg,系统 MUST 将迁移前散落/旧布局下的文档迁移到 `docs/doc/`，并在迁移完成后删除旧路径以避免双真源。
scenarios[5]{req_id,id,given,when,then}:
  r1,"build-succeeds","",开发者运行 docs build 命令（Zensical）,构建 MUST 成功完成
  r2,"docs-root-is-docs-doc","","docs-site 被配置","配置的 `docs_dir` MUST 解析到 `docs/doc/`（例如 `docs/zensical.toml` 在 `docs/` 下配置 `docs_dir = \"doc\"`）"
  r3,"site-output-path-is-stable","","docs-site 构建完成",输出目录 MUST 位于 `docs/site/`
  r4,"specs-and-change-artifacts-are-out-of-scope","","docs-site 构建或 serve",nav MUST NOT 引用来自 `openspec/specs/**` 或 `openspec/changes/**` 的页面
  r5,"old-paths-are-removed","",开发者完成 docs 迁移,旧路径下的同名文档 MUST 不再存在（已移动到 `docs/doc/`）
