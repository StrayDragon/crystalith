kind: llman.sdd.spec
name: "deep-research-runtime"
purpose: ResearchRun HTTP/SSE 运行时：创建/列表/图命令口/报告/revisions 与编排约束（对齐 Lab Compose 外网优先默认）
valid_scope[2]: src/,tests/
requirements[36]{req_id,title,statement}:
  r300,ResearchRun is process SSOT,系统 MUST 以 notebook 作用域的 ResearchRun 作为深研过程 SSOT（图、checkpoint、权威报告与 citation map 挂在 Run 上）；MUST NOT 把长跑过程态的唯一权威塞进 Output 行。
  r301,Create replaces 501 stub,"POST /v2/notebooks/:nid/research MUST 在合法 notebook 下创建 ResearchRun 并返回可追踪 id 与初始状态；MUST NOT 再以 501 NOT_IMPLEMENTED 作为成功创建路径。"
  r302,List and get runs for task inbox,系统 MUST 提供 notebook 作用域的 listRuns 与 getRun；缺少 notebook 作用域时 MUST 拒绝全表列举。
  r303,Shared webSearch tool only,深研 Runtime 的外网检索 MUST 复用既有 searchWeb/SearXNG 实现；MUST NOT 在 research 模块内复制第二套 SearXNG fetch。
  r304,H1 explicit source and web toggles,创建 ResearchRun 时 MUST 接受显式 useNotebookSources 与 allowWeb；省略时默认 useNotebookSources=false、allowWeb=true（外网优先，对齐 Lab Compose）；sourceIds MUST 仅表示深研配置台内选择、MUST NOT 隐式绑定工作区勾选状态；当 useNotebookSources 为 true 且 sourceIds 为空时 MUST 拒绝创建；当 useNotebookSources 与 allowWeb 皆为 false 时 MUST 拒绝创建；需要笔记本来源时客户端 MUST 显式传 useNotebookSources=true。
  r305,L1 depth tiers,系统 MUST 支持浅/中/深三档深度并映射为 maxSearches 与 maxNodes（浅 8/12、中 20/30、深 40/60）；默认档 MUST 为中。
  r306,M1 confirm gates only,首发待确认 MUST 仅覆盖两类硬停：外网预算将尽与主控扩展新支路；系统 MUST 提供继续/出报告或批准支路/跳过收束的确认动作；MUST NOT 恢复逐步审批计划或强制转化弹窗。
  r307,Convert to note is PARAGRAPH with footnotes,显式 convertToNote MUST 将选定产物写入 Markdown PARAGRAPH Output；引用 MUST 按全局 citation map 序列化为 GFM 脚注形式；MUST NOT 在完成时自动写入笔记。
  r308,Convert to source is retrievable,显式 convertToSource MUST 将来源入库并进入可嵌入索引路径，使得后续对话检索能够命中转化内容。
  r309,Report uses structured citations,"Run 权威报告 MUST 为 LLM 结构化成稿（同形 ResearchReport：正文 sections + 全局 citation map）；citation map 可复用 Citation 字段；交互预览以该 SSOT 为准，笔记导出仅为投影；MUST NOT 以启发式证据清单冒充成功成稿。"
  r310,Interactive prune and fork on live runs,在 running 或 awaiting_confirm 状态时系统 MUST 支持对研究图节点的剪枝与 fork；剪枝语义 MUST 遵循 r316（排他父闭包、禁 sink、保留失败 merge）；fork MUST 与 M1 扩展支路共用能力面；completed/failed/cancelled 时图 MUST 只读且 MUST NOT 在同一 Run 上继续扩展（改方向须新 Run，除非后续 P2 明确允许从节点 fork 新 Run）。
  r311,R2a status machine and R3b SSE,ResearchRun MUST 使用状态 queued|running|awaiting_confirm|completed|failed|cancelled；GET stream MUST 发出 status、graph_patch、confirm、report_ready、log、error、progress 事件（R3b）；MUST NOT 对流式推送报告正文 token。
  r312,Cancel A1 and checkpoint CP1,系统 MUST 提供协作 cancel：尽量落盘 checkpoint 并将状态置为 cancelled；checkpoint MUST 在每完成一图节点以及进入 awaiting_confirm 前强制落盘（CP1）。
  r313,"EV1 run-scoped evidence store",系统 MUST 为每个 ResearchRun 维护作用域内证据库（web 与 chunk 统一为 evidence 行并暴露稳定 id）；convert 目标 kind=evidence 时 MUST 引用该库；MUST NOT 仅用不稳定 URL hash 冒充首发 evidenceId SSOT。
  r314,"ERR-G1 AppHttpError codes","深研 HTTP 错误 MUST 走 AppHttpError/ErrorEnvelope；除既有通用 ErrorCode 外 MUST 仅新增 RESEARCH_INVALID_STATE 与 RESEARCH_BUDGET；MUST NOT 另建一整套 research-only error 命名空间。"
  r315,D1 graph_patch and node edge fields,SSE graph_patch MUST 以增量 upsert nodes/edges 与 removeNodeIds/removeEdgeIds 表达；节点 MUST 携带 conclusionStatus（clear|partial|missing|pending|pruned）与可选 phase；边 MUST 使用闭集 EdgeKind（R5）并可带可选 labelNote。
  r316,"Prune closure exclusive-parent B",剪枝 MUST 计算排他父闭包：从目标研究节点出发仅沿非 merge 边向下扩展；子节点仅当其全部非保护入边父节点已在闭包内或已为 pruned 时方可纳入；闭包 MUST NOT 包含 root/question/conclusion（或 id 前缀 node_root_/node_conclusion_）；MUST NOT 沿 merge 边级联；对保护节点的 prune 请求 MUST 拒绝（INVALID_REQUEST 或等价）；被纳入闭包的节点 MUST 标记 conclusionStatus=pruned，且其既有 merge 入结论边 MUST 保留（失败汇入），MUST NOT 仅因剪枝删除该 merge。
  r317,"Single-sink DAG with node role",ResearchRun 图 MUST 维护单一 question 与单一 conclusion（role 或兼容 id 前缀）；研究支路 MUST 经非 merge 边扩展，并对结论使用 merge 边汇入；系统 MUST NOT 在同一 Run 内创建第二个 conclusion sink；新写入节点 SHOULD 携带 role（question|research|conclusion），读取时语义以 role 为准并兼容历史 node_root_/node_conclusion_ 前缀。
  r318,"Serial work-unit kernel CP1 and abort","深研编排 MUST 以 ResearchRun 上的串行 work-unit 内核推进（种子图、节点检索/综合、结案出报告）；每完成一单元以及进入 awaiting_confirm 前 MUST 落盘 checkpoint（CP1）；开始单元前 MUST 重读图并跳过 conclusionStatus=pruned 的节点（discard-if-pruned）；cancel 与剪除当前活动节点时 MUST 中止进行中的单元（AbortSignal 或等价）并保持图 SSOT 在服务端；若存在进行中的节点 chat MUST 一并中止。"
  r319,Fork approve merges into conclusion,当 confirmKind=expand_branch 且用户 approve_branch 时，系统 MUST 新增 research 角色节点，MUST 建立从父节点出发的 fork 或 expand 边，并 MUST 建立该节点到唯一 conclusion 的 merge 边；MUST NOT 仅添加无汇入结论的线性子节点作为唯一结果；skip_branch MUST 不新增节点并允许内核继续。
  r320,"Command-port mutations only",研究图的权威突变 MUST 仅通过服务端命令口完成（create/confirm/cancel/prune/fork/节点 PATCH/revisions 恢复）；系统 MUST NOT 提供客户端整图上传作为 SSOT；剪枝闭包与拓扑约束 MUST 在服务端计算与强制；节点 chat MUST NOT 在未确认的情况下直接改图。
  r321,PATCH node fields on live runs,在 running 或 awaiting_confirm 时系统 MUST 支持 PATCH 节点的 title、query、conclusionStatus（请求体至少一项）；对已 pruned 或受保护的 question/conclusion 节点 MUST 拒绝；完成后 MUST 经 graph_patch 或返回 Run 反映权威状态。
  r322,Node chat SSE proposals only,"系统 MUST 提供 POST 节点 chat 的短轮 SSE（正文增量与可选 ActionProposal/tool-approval）；chat MUST 与 Run 进度 stream 分离；同一 Run 上 chat 与自研 work-unit MUST 互斥（拒绝或排队）；Structure 类意图 MUST 经用户确认后调用既有命令口，MUST NOT 在 tool execute 中静默改图。"
  r323,"User-visible run revisions",系统 MUST 为 ResearchRun 提供用户可见的 revisions：至少支持列表、从当前 graph 与权威 report 创建快照、按 id 读取；恢复某 revision MUST 将对应 graph/report 写回为当前权威并通知客户端（graph_patch 或等价）；revisions MUST NOT 与内部 checkpoint 混为同一概念。
  r324,Align ResearchRun contract to Lab inventory,"系统 MUST 根据 c80 inventory 中标为 fix-api 的项修正 ResearchRun Zod 与 HTTP/SSE 行为，使命令口与 Lab 设计语义一致；MUST 在本变更内使 apps/server/tests/research 全绿；MUST NOT 删除 /v2/notebooks/:nid/research* 主路由面。"
  r325,"Lab-only extras stay documented not silent","对 inventory 标为 lab-only/no-api 的能力，系统 MUST 在合约或 inventory 中显式记录为非 API 权威，MUST NOT 用未文档化的 501 或静默丢字段冒充已支持；标为 defer 的项 MUST 列出后续 change。"
  r326,"Topic auto-decompose after seed","seed 单结论 DAG 完成后，系统 MUST 通过结构化 LLM 规划将主题自动拆解为 research 角色节点：自 question 以 decompose/refine 边扩展，各活研究节点 MUST 以 merge 边汇入唯一 conclusion；节点总数 MUST 受 Run.maxNodes（depth→r305）约束；规划失败或输出非法时 MUST 回退为仅 question work-unit 路径且 MUST NOT 伪造多支路拓扑；拓扑权威 MUST 来自服务端 graph_patch 与 progress ledger，MUST NOT 以前端 timer/phase 为 SSOT；LLM MUST NOT 直接 mutate 图，仅允许经内核规划写回后 emit graph_patch。"
  r327,"Serial branch work-units","系统 MUST 对图中全部非 pruned 的 research 角色节点（不仅 question）串行执行 work-unit：检索/综合、经 writeBackNodeWork 写回 evidence 与 phase，并经 graph_patch 推送增量；调度 MUST 跳过 conclusionStatus=pruned 节点；完成分支波次后 MUST 按既有 M1 语义进入 budget confirm 与 synthesize 收束；MUST NOT 为收束便利删除 research→conclusion 的 merge 边；每单元 MUST 写入 progress ledger（unit_started/unit_finished/unit_skipped_pruned）。"
  r328,"LLM synthesize report no heuristic success","结案 MUST 经结构化 LLM 产出 ResearchReport 并在成功时将 Run 置为 completed；当结案模型调用失败或 cite 校验判定 synthesize_failed 时 MUST 将 Run 置为 failed 并暴露 failureReason；MUST NOT 在成功路径使用启发式证据清单作为权威报告；报告正文 token MUST NOT 进入 Run SSE（仍仅 report_ready + GET）。"
  r329,"Retry synthesize with optional modelId","系统 MUST 提供对结案失败 Run 的 retry-synthesize 命令口（POST …/research/:rid/retry-synthesize 或等价）；请求 MAY 含 modelId 并 MUST 写回 Run；重试 MUST 复用已有 graph 与 evidence 且 MUST NOT 重跑 decompose 或支路 work-unit；成功 MUST completed+report，再次失败 MUST 保持 failed；对非结案失败态 MUST 拒绝（RESEARCH_INVALID_STATE 或等价）。"
  r330,"Run modelId on create and synthesize","创建 ResearchRun 时 MUST 接受可选 modelId 并持久化；结案与 retry-synthesize MUST 使用 Run.modelId（省略时回落默认 chat/config）；retry 覆盖的 modelId MUST 写回 Run；GET Run MUST 暴露 modelId（若有）。"
  r331,"Citation bind sufficient not necessary","结案 cite key MUST 绑定本 Run 证据映射；非法/悬空 key MUST 剥离；若模型声称引用且有效 cite 为零（全非法）则 MUST synthesize_failed；有证据却 0 cite、以及 0 证据下的诚实成稿 MUST 允许 completed；引用 MUST NOT 被当作节点或报告结论的必要条件。"
  r332,"Node short synthesis no fake hits","支路 work-unit MUST 在检索后经 LLM 短综合写回节点摘要；空证据列表 MUST 合法；MUST NOT 使用 pragmatic 假命中回退冒充检索结果；当节点模型彻底失败无法产出结论时 MUST 标记该节点 conclusionStatus=missing（或等价）并继续其它支路，MUST NOT 仅因此将整 Run 立即 failed。"
  r333,"No automatic secondary decompose","在无用户显式请求或 confirm 批准的情况下，内核 MUST NOT 在同一 ResearchRun 上自动执行第二次 topic decompose；首次 seed 后拆解（r326）不受本条禁止。"
  r334,"Confirm-gated request-reexpand","系统 MUST 提供 request-reexpand 命令口（POST …/request-reexpand 或等价），使 Run 进入 awaiting_confirm 且 confirmKind=reexpand；用户 approve_reexpand MUST 经结构化规划追加 research 节点（受 maxNodes）并 graph_patch，skip_reexpand MUST 不新增支路并允许继续收束；规划失败 MUST NOT 伪造多支路；MUST NOT 在 tool execute 中静默改图。"
  r335,"Revision fork creates new Run","系统 MUST 提供从某 revision 派生新 ResearchRun 的命令口（POST …/revisions/:revId/fork-run 或等价）；新 Run MUST 拷贝该 revision 快照的 graph 与 report（若有）以及源 Run 的 topic/预算/modelId 等创建配置，searchesUsed MUST 重置为 0 且初始 status MUST 为 queued；源 Run 的图/报告/revisions MUST 保持不变；默认 MUST NOT 自动 schedule；可选 schedule 或独立 schedule 口 MAY 仅对 queued Run 启动内核；图中 evidence 引用 MUST 在新 Run 作用域内可用（拷贝并 remap 或等价）。"
scenarios[57]{req_id,id,given,when,then,feature}:
  r300,"run-holds-graph-and-report",用户已创建一次深研 Run,系统推进研究并生成报告,图与权威报告及 citation map 均挂在该 ResearchRun 上，而非仅存在于 Output 行,true
  r301,"create-returns-run-id",notebook 存在且请求合法,客户端 POST 创建深研,系统返回 ResearchRun id 与初始状态且不得以 501 作为成功路径,true
  r302,"list-requires-notebook",系统中存在多个 notebook 的 Run,客户端省略 notebook 作用域调用 list,系统拒绝全表列举,true
  r303,"websearch-shared",顶栏直接搜索与深研均需外网检索,深研 Runtime 发起 web 搜索,走与 sources 网搜相同的 searchWeb 实现,true
  r304,"toggles-require-explicit-selection",用户打开用来源开关但未在深研台勾选来源,尝试创建深研 Run,系统拒绝创建且不得静默改为仅外网,true
  r304,"omit-defaults-web-first",客户端仅传 topic、省略通道字段,POST 创建深研,Run 的 useNotebookSources=false 且 allowWeb=true,true
  r305,"default-medium-tier",用户未指定深度档位,创建深研 Run,系统使用中档预算 maxSearches=20 与 maxNodes=30,true
  r306,"budget-confirm",Run 外网预算将尽,主控需要继续消耗搜索额度,进入待确认并提供继续或出报告动作,true
  r307,"convert-note-footnotes",Run 已有带 citation map 的终稿,用户显式 convertToNote,写入 PARAGRAPH 且正文含 GFM 脚注投影,true
  r308,"convert-source-rag",用户将证据或报告转为来源,转化完成并完成索引,对话检索可以命中转化内容,true
  r309,"structured-report-ssot",报告已合成,客户端读取 Run 报告,返回结构化章节与全局 citation map,true
  r310,"prune-while-running-keeps-merge",Run 处于 running 且某研究节点有 merge 到结论,用户对该节点执行剪枝,该节点（及 r316 闭包内独占下游）标为 pruned，后续编排不再扩展该支路，且 merge 边保留为失败汇入,true
  r311,"stream-status-events",客户端已订阅 Run 的 SSE stream,Run 从 queued 进入 running,客户端收到 status 事件且可继续接收 graph_patch、log 与 progress,true
  r312,"cancel-persists-checkpoint",Run 正在 running 且已有节点完成,用户调用 cancel,状态变为 cancelled 且最近 checkpoint 已落盘,true
  r313,"evidence-row-for-convert",Run 已收集一条 web 证据,用户以 kind=evidence 调用 convertToSource,转化引用该 evidence 稳定 id 且入库可检索,true
  r314,"invalid-state-code",Run 已 completed,客户端对该 Run 调用 prune,返回 RESEARCH_INVALID_STATE 且图保持只读,true
  r315,"graph-patch-upsert",客户端已订阅 stream,主控新增一节点与一条边,收到 graph_patch 含对应 nodes 与 edges upsert,true
  r316,"shared-child-stays-live",图中 libs 与 perf 均指向共享子节点 stream，且 libs 另有 merge 到 conclusion,用户对 libs 执行 prune,libs 标为 pruned；stream 因仍有活父 perf 保持未剪枝；conclusion 不被剪枝；libs→conclusion 的 merge 边仍在,true
  r316,"exclusive-child-cascades",data 仅有独占子 minimize，minimize merge 到 conclusion,用户对 data 执行 prune,data 与 minimize 均标为 pruned；minimize→conclusion 的 merge 边保留；conclusion 不被剪枝,true
  r316,"reject-protected-root",Run 处于 running 且存在 node_root_*,客户端对该 root 调用 prune,请求被拒绝且图不变,true
  r317,"seed-has-one-conclusion",Run 刚进入 running,内核完成 seed_graph,图中恰好一个 question 与一个 conclusion（或兼容前缀），且二者可被客户端识别,true
  r318,"skip-pruned-unit",某 research 节点已被 prune 且仍排在调度队列,调度器准备执行该节点单元,单元被跳过且不写入新摘要，SSE 可继续推进其他活节点,true
  r318,"cancel-aborts-active",Run 正在执行某节点的检索或综合,用户调用 cancel,活动单元中止，状态进入 cancelled（或协作收尾至 cancelled）且 checkpoint 已落盘,true
  r319,"approve-adds-merge",Run 处于 awaiting_confirm 且 confirmKind=expand_branch,用户 approve_branch,新 research 节点存在，并有指向唯一 conclusion 的 merge 边,true
  r320,"reject-client-authored-closure",客户端试图用非命令口方式提交任意节点集合为权威剪枝结果,调用未合约化的整图写入或等价接口,请求被拒绝或不存在该接口，图仅能经 prune 等命令口变更,true
  r321,"patch-query-on-research-node",Run 为 running 且存在可编辑 research 节点,客户端 PATCH 其 query,返回更新后的 Run 或随后 graph_patch 含新 query,true
  r322,"chat-does-not-auto-prune",节点 chat 流提出 prune 提案,用户尚未确认,图上该节点仍未 pruned,true
  r322,"chat-separate-from-run-stream",客户端已订阅 Run stream,发起节点 chat,chat 正文事件不出现在 Run stream 事件名集合中,true
  r323,"create-and-list-revision",Run 已有 graph 与 report,用户创建 revision 后列表,列表含新快照且可读回 graph/report 内容,true
  r324,"research-tests-green-after-align",c81 合约修正完成,运行 server research 测试套件,全部通过且 research 路由仍可创建 Run,true
  r325,"lab-only-capability-documented",某 Lab 能力不进 API,查阅 inventory/合约,"该能力被标为 lab-only 或 defer，而非伪装成功",false
  r326,"decompose-emits-branches-and-merges",Run 已 seed 且 planner 成功,内核完成拆解,graph 含多个 research 节点、question 发出 decompose 边且各活节点有 merge 至 conclusion,true
  r326,"planner-failure-falls-back-question-only",planner 返回非法或解析失败,runLoop 继续,仅 question 路径执行工作单元且不得假装多分支已成功,true
  r326,"max-nodes-respected",Run depth=shallow 且 maxNodes=12,planner 提议超限支路,服务端裁剪或拒绝扩展且节点数不超过预算,true
  r327,"drain-all-research-nodes",Run 经拆解含三个活 research 节点,runLoop 执行 drain,"每个节点均完成 work-unit 且 evidenceIds 写回",true
  r327,"skip-question-when-branches",Run 已拆解出活 research 节点,runLoop 调度,"跳过 question work-unit 且各 research 完成写回",true
  r327,"skip-pruned-branch-unit",其中一支已被 prune,调度器 drain,pruned 节点被跳过且其余节点仍执行,true
  r327,"merge-edges-survive-synthesize",各支路已写回证据,Run 进入 synthesize 并完成,所有活 research 至 conclusion 的 merge 边仍存在,true
  r328,"llm-report-on-success",结案 LLM 返回合法 ResearchReport,synthesizeAndComplete 完成,"Run status 为 completed 且 report 非证据清单启发式",true
  r328,"synthesize-fail-no-heuristic",结案模型调用抛错,观察 Run,"status 为 failed 且无启发式权威报告",true
  r329,"retry-synthesize-succeeds","Run 因结案失败为 failed 且已有 evidence",客户端 POST retry-synthesize,"Run 进入短窗后 completed 且含报告",true
  r329,"retry-does-not-rerun-drain",failed 结案 Run 已有支路 evidence,retry-synthesize,"不新增 research work-unit 检索进度且复用原 evidence",true
  r329,"retry-rejects-completed",Run 已 completed,调用 retry-synthesize,"返回 RESEARCH_INVALID_STATE 或等价",true
  r330,"create-persists-model-id",客户端 POST create 含 modelId,GET Run,返回相同 modelId,true
  r331,"zero-evidence-honest-complete","Run 证据为 0 且结案 LLM 产出无 cite 报告",synthesize 完成,status 为 completed,true
  r331,"all-illegal-cites-fail",结案 LLM 输出的 cite key 均不在证据映射,校验后,"Run failed 且可 retry",true
  r332,"empty-evidence-node-ok",research 节点检索无命中,短综合完成,"节点可有摘要且 evidenceIds 可空",true
  r332,"node-model-fail-missing",节点短综合模型失败,写回,"该节点 conclusionStatus 为 missing 且其它支路可继续",true
  r333,"no-auto-second-decompose",Run 已完成首次拆解并进入预算确认或结案前,等待且无 request-reexpand,图上 research 节点数不因内核自动再拆而增加,true
  r334,"request-reexpand-awaits-confirm",Run 为 running 或 awaiting_confirm,客户端 POST request-reexpand,status 为 awaiting_confirm 且 confirmKind=reexpand,true
  r334,"approve-reexpand-adds-branches",confirmKind=reexpand,用户 approve_reexpand 且 planner 成功,图新增 research 节点与 merge 边,true
  r334,"skip-reexpand-no-nodes",confirmKind=reexpand,用户 skip_reexpand,research 节点数不增加,true
  r334,"reexpand-planner-failure",confirmKind=reexpand 且 planner 失败,approve 处理完成,不得伪造多支路拓扑,true
  r335,"fork-creates-new-run-id",源 Run 已有 revision 快照,客户端 POST fork-run,返回新 ResearchRun id 且 status=queued,true
  r335,"fork-copies-graph-report",revision 含 graph 与 report,fork 成功,新 Run 的 graph/report 与该 revision 快照一致,true
  r335,"fork-leaves-source-unchanged",fork 完成后,再次 GET 源 Run 与其 revisions 列表,与 fork 前一致,true
  r335,"fork-default-no-schedule",客户端 fork 且省略 schedule 或 schedule=false,观察新 Run,保持 queued 且不因 fork 立刻进入 running,true
