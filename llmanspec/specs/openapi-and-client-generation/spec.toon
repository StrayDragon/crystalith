kind: llman.sdd.spec
name: "openapi-and-client-generation"
purpose: 定义 OpenAPI 暴露与多端客户端生成链路，确保后端 schema、前端生成客户端与 Python SDK 一致。该规范用于约束“契约是单一真源”，避免多处手写接口导致漂移。
valid_scope[2]: src/,tests/
requirements[6]{req_id,title,statement}:
  r1,OpenAPI JSON and UI endpoints are exposed,系统 MUST 提供 OpenAPI JSON 与可交互 UI 端点。
  r2,OpenAPI is the single source of truth for generated clients,前端生成客户端与 Python SDK MUST 基于同一 OpenAPI 生成。
  r3,Generated frontend client is default path,前端新增 API 调用 MUST 优先使用生成客户端；手写调用仅限 SSE/流式等例外。
  r4,Python SDK generation is deterministic,Python SDK 生成流程 MUST 可重复，并与后端版本保持一致。
  r5,CI validates schema/client/sdk freshness,CI MUST 校验 OpenAPI、前端生成代码、SDK 产物无漂移。
  r6,OpenAPI schema export artifact is committed at a stable path,仓库 MUST 将后端 OpenAPI schema 的导出产物固定为 `frontend/web/openapi.gen.json`，并将其作为前端生成客户端与各语言 SDK 生成的唯一输入。
scenarios[6]{req_id,id,given,when,then}:
  r1,"operator-accesses-api-docs","",开发者或操作者访问 API 文档入口,系统 SHALL 提供 OpenAPI JSON 与可交互 UI 端点
  r2,"generate-clients-from-the-same-schema","",执行前端客户端与 Python SDK 生成,两者 SHALL 基于同一 OpenAPI schema 产出
  r3,"add-a-new-frontend-api-call","",前端新增一个非 SSE/流式的 API 调用,该调用 SHALL 通过生成客户端完成而非手写请求
  r4,"re-running-sdk-generation-is-stable","",在同一后端版本上重复运行 SDK 生成流程,产物 SHALL 保持可重复并与后端版本一致
  r5,"drift-is-detected","",OpenAPI 或生成客户端/SDK 与仓库提交内容不一致,CI SHALL 检测到漂移并失败提示
  r6,"generation-uses-the-canonical-exported-schema","",开发者执行 OpenAPI 导出与客户端/SDK 生成,生成链路 SHALL 以 `frontend/web/openapi.gen.json` 作为唯一 schema 输入
