kind: llman.sdd.spec
name: "multi-notebook-collections"
purpose: "DEFERRED（无 collection 域实现）：在 notebook 之上提供聚合层 collection：通过绑定多个 notebooks 形成更大的工作上下文，并提供 collection-scoped 的检索与导航语义，同时保留 notebook 的身份与来源归属边界。本能力当前仅规范愿景，不以代码为 SSOT 验收。"
valid_scope[2]: src/,tests/
requirements[4]{req_id,title,statement}:
  r36,collection 必须通过绑定 notebook 构成工作现场,系统 MUST 以 notebook 绑定关系来定义 collection，而不是让 collection 脱离 notebook 独立吞并内容。
  r94,"collection 必须提供 collection-scoped 检索与导航",系统 MUST 为 collection 提供独立的检索与导航语义，以支持跨 notebook 工作。
  r131,collection 不得吞掉 notebook 原生语义,系统 MUST 保证 collection 是空间放大层，而不是 notebook 的替代品。
  r167,系统必须提供 collection 的管理与 notebook 绑定接口,系统 MUST 提供 collection 的列表/详情/更新能力，并允许用户将多个 notebook 绑定到同一个 collection 作为工作上下文。
scenarios[5]{req_id,id,given,when,then,feature}:
  r36,"用户创建一个-collection","",用户创建一个 collection,系统 SHALL 允许其绑定多个 notebook,true
  r94,"用户在-collection-内查找信息","",用户进入某个 collection 并发起检索或导航,系统 SHALL 在 collection 绑定范围内工作,true
  r131,"用户从-collection-回到-notebook","",用户在 collection 中查看某条结果或对象,系统 SHALL 能回溯其所属 notebook,true
  r167,"用户将-notebook-绑定到-collection","",用户把一个 notebook 加入某个 collection,系统 SHALL 更新该 collection 的绑定关系,true
  r167,"用户从-collection-中移除-notebook","",用户将某个 notebook 从 collection 中移除,系统 SHALL 更新绑定关系并使其不再参与该 collection 的检索与生成,true
