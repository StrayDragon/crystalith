kind: llman.sdd.spec
name: "knowledge-curation-and-freshness"
purpose: 为长期知识库提供治理信号与维护建议：freshness（新鲜度/过时风险）、duplicate candidates（重复候选）、maintenance suggestions（维护建议）与显式动作（ignore/re_ingest/re_embed/review），避免知识库随时间劣化。
valid_scope[2]: src/,tests/
requirements[15]{req_id,title,statement}:
  r35,来源对象必须暴露 freshness 信号,系统 MUST 为接入后的来源对象提供 freshness 信号，以支持长期维护。
  r93,系统必须提供重复候选而不是自动合并,系统 MUST 以候选形式呈现重复内容判断，而不是直接自动合并对象。
  r130,维护建议必须显式触发后续动作,系统 MUST 将维护建议与后续动作解耦，避免治理信号直接隐式改写来源状态。
  r166,"治理信号必须绑定到稳定的来源对象（source-scoped）",系统 MUST 将 freshness 与 duplicate 等治理信号绑定到稳定的来源对象标识，而不是绑定到一次性的导入请求或临时 UI 状态。
  r201,系统必须提供显式动作以处理治理建议,系统 MUST 提供显式动作来处理 freshness/duplicate 建议（例如 ignore、re_ingest、re_embed、review），并在触发后产生可追踪的处理结果。
  r230,工作区必须提供可聚合查看的治理入口,系统 MUST 在工作区/来源管理界面提供治理入口，聚合展示 stale sources、duplicate candidates 与维护建议，避免治理能力藏在单个来源详情里。
  "research-dedup-must-be-cross-iteration","Research dedup MUST seed seen-set from all accumulated results across iterations","research 搜索结果去重 MUST 跨迭代累积：去重 seen-set（seen_titles/seen_normalized_urls）SHALL 用 state.all_results（所有先前迭代累积结果）初始化，而非仅当前批次；当前 agent.ts:161 与 178 只对单批 allResults 去重，与 v1 graph.py:518 与 551 的跨迭代去重不一致，导致同一 URL/title 在迭代 2 后重复出现"
  "research-report-prompt-must-be-consistent","Research report generation MUST use the v1 6-section prompt on BOTH paths","research report 生成 MUST 在所有路径使用 v1 的 6 段结构化 REPORT_SYSTEM_PROMPT（graph.py:94-128：Executive Summary/Background/Key Findings/Analysis/Recommendations/References）。当前 v2 仅 finish 路径（generateFinalReport, agent.ts:549-583）用该 prompt；正常完成路径（generateReport, agent.ts:296-304）用 1 行极简 prompt 'You are a research report writer. Produce a comprehensive markdown report.'，与 c46 承诺的 report 富 prompt 仅半数兑现"
  "research-sse-must-emit-status-and-thinking",Research SSE MUST emit status events and rich thinking payloads like v1,"research SSE 事件流 MUST：(1) 在每次状态转换时发 status 事件（含 status/previous/message）；(2) 非具名步骤发 thinking 且 message 优先取 summary/reasoning/insight/decision。实现：research/router.ts 流式 poll（c49 + D.4）。"
  "research-resume-must-restore-plan","Research resume MUST restore the in-flight search plan and start node","research resume（runResearchFromState）MUST 还原暂停时的 search_plan 与起始节点：(1) SHALL 从持久化 steps 还原 search_plan（v1 _extract_plan_from_steps, graph.py:945-972 与 1009）；当前 agent.ts:502-526 只还原 iteration+results，总是重新 planSearches。(2) SHALL 支持 start-node 选择（v1 _start_node_for_status, graph.py:1013-1028）使 waiting_user/searching 状态跳过重规划直接进入 WaitForApproval/ExecuteSearches。否则 resume 一个 waiting_user 会话会重新生成 plan 而非展示用户即将审批的那个"
  "research-export-note-must-use-structured-type",Research export note MUST use STRUCTURED output type with v1 content schema,"research export 的 note 类型 MUST 用 OutputType=STRUCTURED + content={title, text, metadata}（v1 api.py:1434-1448），MUST NOT 用 BRIEFING + {title, sections:[{heading, points}]}（当前 router.ts:606-611）。两者 type 与 content schema 均不兼容，跨版本不可比"
  r_tool_approval,Research HITL via toolApproval,"研究智能体 MUST 使用 AI SDK v7 toolApproval user-approval 实现事件驱动审批 替代 DB 轮询"
  r_resume_with_state,Research resume preserves state,研究 resume MUST 从 aggregatedResults 与 currentIteration 重建 ResearchState 而非从空结果重跑
  r_export_to_source,Research export creates source,研究 export MUST 将 report 经 chunk 与 embed 与 vector 写入并创建 source 而非仅返回原始 JSON
  r_modify_action,Research HITL modify action,研究 MUST 支持 modify 动作 接受修改后的 SearchPlan 记录 step 并 resume
scenarios[16]{req_id,id,given,when,then,feature}:
  r35,用户查看某个来源是否过旧,"",用户查看已接入的来源对象,系统 SHALL 能返回该对象的 freshness 状态或提示,true
  r93,系统发现高相似来源,"",系统识别出可能重复的来源对象,系统 SHALL 返回 duplicate candidates,true
  r130,用户处理维护建议,"",用户收到某个来源的维护建议,系统 SHALL 明确该建议对应的可选动作,true
  r166,用户跨会话查看同一来源的治理状态,"",用户在不同时间打开同一个来源对象,系统 SHALL 能返回该来源对象的 freshness 信号与重复候选状态,true
  r201,用户忽略某条治理建议,"",用户对某个来源的维护建议执行 ignore,系统 SHALL 记录该忽略状态,true
  r201,用户触发重新导入或重新嵌入,"",用户对某个来源触发 re_ingest 或 re_embed,系统 SHALL 以后台任务形式执行该动作或返回可追踪的执行引用,true
  r230,用户在工作区查看需要维护的来源,"",用户进入来源管理或治理入口,系统 SHALL 能展示 stale sources 列表与重复候选列表,true
  "research-dedup-must-be-cross-iteration","cross-iter-dedup",迭代 1 已返回某 URL 的结果,迭代 2 再次返回同一 URL,该 URL SHALL 被去重（与 v1 一致），不重复进入 all_results,true
  "research-report-prompt-must-be-consistent","normal-completion-report",research agent 自然完成最大迭代,系统生成 report,report SHALL 使用 6 段结构化 prompt（与 finish 路径一致），非 1 行极简 prompt,true
  "research-sse-must-emit-status-and-thinking","sse-status-event",research 状态从 planning 转 waiting_user,SSE 流推送事件,SHALL 发 status 事件；前端 SHALL 能据 status 更新 UI 状态,true
  "research-resume-must-restore-plan","resume-with-plan",session 处 waiting_user 且有未审批 plan,客户端触发 resume,SHALL 展示原 plan（而非重新生成）；SHALL 跳过重规划直接进入 WaitForApproval,true
  "research-export-note-must-use-structured-type","export-note",用户导出 research 为 note 类型,系统创建 Output,"Output.type SHALL 为 STRUCTURED + content={title,text,metadata}（与 v1 一致）",true
  r_tool_approval,no_db_polling,研究运行至 plan 阶段,agent 等待用户审批,系统 SHALL 通过 toolApproval 事件而非 DB 轮询阻塞,true
  r_resume_with_state,resume_keeps_results,研究已积累结果后取消,用户请求 resume,系统 SHALL 从上次 iteration 与累积结果续接,true
  r_export_to_source,export_creates_source,研究完成有 final report,用户请求 export,系统 SHALL 创建 source 并 chunk 与 embed 写入,true
  r_modify_action,modify_then_resume,研究等待审批,用户提交修改后的 plan,系统 SHALL 记录修改并 resume 执行新 plan,true
