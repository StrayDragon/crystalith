kind: llman.sdd.spec
name: "quality-gates-for-generation"
purpose: "DEFERRED（无 QualityGate API/类型）：为生成结果提供统一、可解释、可观测的质量门信号：通过/警告/阻断的最小状态、结构化原因与建议动作，并支持跨生成类型复用。本能力当前仅规范愿景。"
valid_scope[2]: src/,tests/
requirements[6]{req_id,title,statement}:
  r42,生成结果必须具备质量门状态,系统 MUST 为生成结果提供可消费的质量门状态，而不是只暴露“是否生成成功”。
  r100,质量门必须返回结构化原因,系统 MUST 为质量门结果返回结构化原因，以支持解释、展示和后续治理。
  r137,质量门必须可跨生成类型复用,系统 MUST 将质量门定义为可复用能力，而不是某个结果类型的私有规则。
  r173,质量门必须在生成完成后接入并写入结果元数据,系统 MUST 在生成完成并完成最小后处理后运行质量门，并将结果写入生成结果元数据后返回给客户端。
  r205,前端必须可统一展示质量告警、阻断原因与建议动作,系统 MUST 让前端可以基于结构化字段统一展示质量告警/阻断原因，并提供建议动作提示，而不是依赖不可解释的文本拼接。
  r233,系统必须记录质量门结果用于观测与治理,系统 MUST 记录每次质量门的结果，以支持按类型与 gate 维度的观测与后续治理策略。
scenarios[6]{req_id,id,given,when,then,feature}:
  r42,结果生成完成后附带质量状态,"",一个生成结果进入完成态,系统 SHALL 为其附带质量门状态,true
  r100,结果触发质量告警,"",某个生成结果触发质量门,系统 SHALL 返回结构化原因,true
  r137,不同生成类型接入同一质量门能力,"",两类不同的生成类型都需要最小质量判断,系统 SHALL 允许它们消费同一套质量门模型,true
  r173,系统在结果返回前运行质量门,"",一个生成结果完成并准备返回,系统 SHALL 运行质量门检查,true
  r205,用户查看带质量告警的结果,"",用户打开一个触发 warn/block 的结果,前端 SHALL 能展示触发的质量门列表与原因,true
  r233,运营或开发查看质量门趋势,"",需要分析某段时间内质量告警/阻断趋势,系统 SHALL 能按 gate_id 与 generation_type 维度聚合统计 pass/warn/block,true
