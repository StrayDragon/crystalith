kind: llman.sdd.spec
name: "studio-output-types"
purpose: 定义非 slides 的 Studio 输出类型交互基线（briefing/guide/flashcard/mindmap/quiz/timeline）。该规范以“最小可用交互”约束各类型的前端呈现，保证输出可浏览、可操作与可导出。
valid_scope[2]: src/,tests/
requirements[10]{req_id,title,statement}:
  r52,Briefing supports section structure and fold,briefing 输出 MUST 支持章节目录与章节折叠。
  r110,Guide supports checklist and progress cues,guide 输出 MUST 支持可勾选学习路径与进度提示。
  r147,Flashcard supports flip and navigation,flashcard 输出 MUST 支持翻面交互与学习进度导航。
  r182,"Mindmap supports read-only exploration",mindmap 输出 MUST 支持只读浏览与节点折叠控制。
  r214,Quiz supports answering and immediate feedback,quiz 输出 MUST 支持作答、即时反馈与结果汇总。
  r241,Timeline supports visual events and detail expansion,timeline 输出 MUST 支持时间轴事件可视化与事件详情展开。
  r259,"Studio output types are plugin-driven and discoverable","Studio 的工具输出类型集合 MUST 由 `/v1/workspace/tools` 返回的动态列表驱动；客户端 MUST NOT 依赖硬编码枚举来假设某输出类型必然存在。 说明： - tools 列表的来源可以是 core 内置能力或插件能力 - 在本变更范围内，除 `SLIDES`（暂保留 core 内置）外，其余工具输出类型（FAQ/GUIDE/TIMELINE/MINDMAP/QUIZ/BRIEFING）MUST 由官方插件提供"
  r271,Missing capabilities are explained to the user,当某输出类型不可用（插件缺失/禁用/加载失败）时，客户端 MUST 能呈现可执行的恢复提示（例如安装/启用对应官方插件），以避免“功能消失但原因不明”。
  "studio-output-sync-must-write-output-id-fk",Studio output sync MUST write studioSlides.outputId FK and full content schema,"studio 的 syncSlideOutput MUST (1) 写 studioSlides.outputId 外键（当前 v2 从不写该列，schema.ts:361 的列恒为 null），(2) content schema 对齐 v1 _sync_output（api.py:194-229）：content={title, engine, outline, markdown, slide_id}；当前 v2 service.ts:133-157 只写 {title, markdown, stage:'markdown'}（缺 engine/outline/slide_id）且用 prompt='studio:<id>' 匹配而非用 FK。任何依赖 slide→output join 的 UI/查询在 v2 失效"
  "studio-stale-cleanup-must-not-set-error-message","Studio stale-RUNNING cleanup MUST clear error and return bool like v1","studio 的 clearStaleRunning MUST 对齐 v1 _clear_stale_running_status（api.py:107-118）：设 status=IDLE、清空 error_message（设 None）、返回 bool；当前 v2 service.ts:105-118 把 errorMessage 设为 'stale running status cleared'（非空字符串），导致 stale 清理后的 slide 在 v2 显示错误信息而 v1 不显示"
scenarios[10]{req_id,id,given,when,then,feature}:
  r52,"briefing-sections-are-collapsible","",用户查看 briefing 输出并操作某一章节,系统 SHALL 支持章节目录导航与章节折叠/展开,true
  r110,"guide-checklist-shows-progress","",用户在 guide 中勾选学习步骤,系统 SHALL 更新进度提示并保持交互可用,true
  r147,"flashcard-flip-and-navigate","",用户在 flashcard 上执行翻面与上一张/下一张操作,系统 SHALL 提供翻面交互并支持进度导航,true
  r182,"mindmap-nodes-can-be-expanded-collapsed","",用户浏览 mindmap 并操作某节点,系统 SHALL 支持只读探索与节点折叠/展开,true
  r214,"quiz-gives-feedback","",用户提交某题答案,系统 SHALL 提供即时反馈并可查看结果汇总,true
  r241,"timeline-details-expand","",用户点击时间轴事件,系统 SHALL 展示事件详情并保持时间轴可继续浏览,true
  r259,"output-type-options-are-derived-from-tools-api","",客户端加载 Studio 的输出类型选择器,可选项 SHALL 来自 `/v1/workspace/tools` 返回的 tools 列表,true
  r271,"ui-shows-an-actionable-hint-for-missing-tool","",tools API 返回结构化诊断信息指出某输出类型插件不可用,UI SHALL 显示该能力不可用原因,true
  "studio-output-sync-must-write-output-id-fk","output-fk-join",前端或查询用 studioSlides.outputId join outputs,系统 syncSlideOutput,studioSlides.outputId SHALL 被写入；content SHALL 含 engine/outline/markdown/slide_id,true
  "studio-stale-cleanup-must-not-set-error-message","stale-no-error",slide 处 RUNNING 但已 stale（>10min）,系统清理 stale 状态,status=IDLE 且 error_message 为空（非 'stale running status cleared'）,true
