kind: llman.sdd.spec
name: "generation-variants-and-comparison"
purpose: "DEFERRED（无 variants 产品面）：为同一生成意图提供可选的多候选（variants）生成与比较/选定语义：共享意图与控制配置、提供结构化比较信息、显式选定主结果并保留可追溯选择历史。本能力当前仅规范愿景。"
valid_scope[2]: src/,tests/
requirements[7]{req_id,title,statement}:
  r34,系统必须支持在同一生成意图下生成多个 variant,系统 MUST 支持在同一生成类型与共享上下文下生成多个可比较候选结果。
  r92,系统必须提供结构化比较信息,系统 MUST 为多个 variant 提供结构化比较信息，以帮助用户完成选择。
  r129,用户必须能够选定工作结果,系统 MUST 允许用户从 variant 集合中显式选定一个工作结果，而不是让所有候选长期并列悬空。
  r165,variant 流程必须是可选且受限的观察项,系统 MUST 将 variant 作为可选流程提供，并保持默认单结果路径不变；同时系统 MUST 对 variant 数量与成本进行显式约束与提示。
  r200,同一 VariantSet 内的候选必须共享生成意图与控制配置,系统 MUST 确保同一 VariantSet 内的所有候选共享同一 `generationTypeId`、同一输入上下文边界与同一控制配置（effective controls），避免候选之间因控制面漂移而不可比。
  r229,多 variant 生成必须共享检索/上下文快照以控制成本,系统 MUST 支持在同一 VariantSet 内复用共享检索结果或上下文快照，使多候选流程主要增加生成成本而非重复检索成本。
  r252,选定关系必须唯一且可追溯,系统 MUST 在一个 VariantSet 中保持唯一选定结果，并记录选定历史以支持回看与改选。
scenarios[10]{req_id,id,given,when,then,feature}:
  r34,用户请求多个候选结果,"",用户在适用场景下启用 variant 生成,系统 SHALL 在同一生成意图下返回多个候选结果,true
  r92,用户比较多个候选结果,"",用户查看一个 variant 集合,系统 SHALL 提供每个候选的结构化比较元数据,true
  r129,"用户确认其中一个-variant","",用户选定一个候选结果,系统 SHALL 记录该选定动作,true
  r165,"默认生成路径不启用-variant","",用户发起一次未显式启用 variant 的生成请求,系统 SHALL 仅返回单个结果,true
  r165,用户显式请求多个候选,"",用户在生成请求中显式指定 variant 数量,系统 SHALL 按请求返回多个候选结果,true
  r165,"系统建议用户尝试-variant","",某次结果触发质量门警告（warn）,系统 SHALL 能向客户端提供“建议启用 variant”的提示信息,true
  r200,"系统生成一个-variantset","",系统为一次生成请求产生多个候选,这些候选 SHALL 共享同一 `generationTypeId`,true
  r229,系统为多个候选复用同一检索结果,"",系统在同一生成意图下生成多个候选,系统 SHALL 复用同一共享检索结果或上下文快照,true
  r252,用户选定一个候选作为主结果,"",用户在一个 VariantSet 中选定某个候选,系统 SHALL 将该候选标记为唯一选定结果,true
  r252,用户改选另一候选,"",用户将选定结果从候选 A 改为候选 B,系统 SHALL 更新唯一选定结果为候选 B,true
