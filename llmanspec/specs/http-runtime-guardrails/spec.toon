kind: llman.sdd.spec
name: "http-runtime-guardrails"
purpose: 定义 HTTP 入口层的最小运行时防护（upload size guard + rate limit），在非本地暴露场景默认开启，避免资源耗尽与误用，同时保持本地开发低摩擦。
valid_scope[2]: src/,tests/
requirements[5]{req_id,title,statement}:
  r1,"HTTP Guardrails MUST Be Configurable and Default to Auto for Non-Local Exposure","系统 MUST 提供可配置的 HTTP guardrails，并支持 `mode=auto|enabled|disabled`。 - `auto`：仅在后端对非 loopback 地址暴露时启用 guardrails。 - `enabled`：无条件启用 guardrails。 - `disabled`：无条件禁用 guardrails。"
  r2,Upload Requests MUST Enforce a Bounded Read Limit When Guardrails Are Enabled,当 guardrails 启用时，上传类请求 MUST 施加 `upload_max_bytes` 上限；超过上限 MUST 返回 413，且该错误 MUST 被视为确定性输入错误（不产生任何来源持久化副作用）。
  r3,HTTP Rate Limiting MUST Return 429 with retry_after When Guardrails Are Enabled,当 guardrails 启用时，系统 MUST 对核心 API surface（例如 `/v1/**`）提供基础 rate limiting；触发时 MUST 返回 429 且暴露稳定的 `retry_after` 指引。
  r4,Health Endpoints MUST Not Be Rate Limited by Guardrails,guardrails 的 rate limiting MUST 不影响探活与诊断用健康检查端点。
  r5,Rate Limit Identity MUST Be Safe by Default,"系统 MUST 提供一个“安全默认”的 client identity 策略用于 rate limit keying，并且默认 MUST 不信任可伪造的代理头（例如 `X-Forwarded-For`）。"
scenarios[8]{req_id,id,given,when,then}:
  r1,"auto-mode-disables-guardrails-for-loopback-bind","",`mode=auto`,系统 SHALL 视为本地开发场景并禁用 guardrails
  r1,"auto-mode-enables-guardrails-for-non-loopback-bind","",`mode=auto`,系统 SHALL 启用 guardrails
  r1,"enabled-overrides-auto","",`mode=enabled`,系统 SHALL 启用 guardrails
  r1,"disabled-overrides-auto","",`mode=disabled`,系统 SHALL 禁用 guardrails
  r2,"oversized-upload-is-rejected-without-creating-a-source-row","",guardrails 启用,系统 SHALL 返回 413
  r3,"client-exceeds-rate-limit","",guardrails 启用,系统 SHALL 返回 429
  r4,"health-endpoints-remain-accessible-under-heavy-load","",guardrails 启用且 rate limiting 生效,`/health` 与 `/health/dependencies` SHALL 不受该 rate limiting 影响
  r5,"proxy-headers-are-ignored-by-default","","客户端请求携带 `X-Forwarded-For`",系统 SHALL 使用直接连接的 client 地址（例如 socket remote addr）作为 rate limit key
