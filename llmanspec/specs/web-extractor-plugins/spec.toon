kind: llman.sdd.spec
name: "web-extractor-plugins"
purpose: "定义网页提取器插件化契约：实现来源、可用性清单、选择与降级、全局/Notebook 级启用策略，以及结构化错误模型，使 URL fetch 能在 core-only 与官方插件套件形态下保持可预测与可诊断。"
valid_scope[2]: src/,tests/
requirements[7]{req_id,title,statement}:
  r55,Web extractors are provided by plugins,网页提取器实现 MUST 由插件提供，core MUST 仅保留提取编排框架（fallback/retry/代理/SSRF 校验）与稳定的数据模型；当某提取器插件未安装或被禁用时，core MUST NOT 视为“内置可用”。
  r113,Extractor selection supports preference and controlled fallback,系统 MUST 支持对单次 fetch 指定 `preferredExtractor`，并 MUST 支持按配置定义的 fallback order；fallback MUST 仅在显式开启时发生，且每次尝试都必须可观测（日志/诊断信息）。
  r150,Extractor availability is checkable and listable,系统 MUST 提供对外可查询的“提取器可用性清单”，包含：提取器类型、是否启用、是否可用、不可用原因与恢复提示（如适用）。
  r185,"Extractor enablement supports global policy and per-notebook policy","系统 MUST 同时支持： - **全局策略（Global Policy）**：由 `config/app.yaml` 给出每个 extractor 的默认启用状态。 - **笔记本策略（Notebook Policy）**：由 UI 通过 API 持久化到数据库（按 notebook），并允许该 notebook 选择“是否遵循全局策略”。 笔记本策略 MUST 支持一个模式字段： - `mode=\"inherit_global\"`：该 notebook 遵循全局策略（默认） - `mode=\"custom\"`：该 notebook 使用自定义启用集合（可与全局不同） effective enabled 的计算 MUST 为： `plugin_enabled (allowlist/denylist) ∧ (mode == inherit_global ? global_enabled : notebook_enabled)` 说明： - 插件启用（install/allowlist/denylist）是运维边界，UI MUST NOT 直接修改。 - `mode=\"custom\"` "
  r217,Extractor plugins provide structured errors,提取器插件在失败时 MUST 抛出结构化错误（至少包含 errorCode/message，且可选包含 extractor/url），以便 core 能区分“可重试失败/不可用/配置缺失”并做出一致的降级与提示。
  "extractors-full-response",GET extractors MUST return full ExtractorsListResponse,GET /extractors MUST 返回完整 ExtractorsListResponse（每提取器 available/displayName/priority/requiresApiKey/recoveryHint + defaultExtractor + fallbackEnabled），MUST NOT 只返回存储的策略行
  r_extractor_outbound_proxy,Extractor network IO uses outboundFetch,Jina/Firecrawl 等需出站 HTTP 的提取器 MUST 经 `outboundFetch`（或等价包装）发出请求，以遵循全局 `proxy_settings`；MUST NOT 在提取器内裸用 `fetch` 绕过代理合约。
scenarios[9]{req_id,id,given,when,then,feature}:
  r55,"extractor-availability-depends-on-installed-enabled-plugins","",运维未安装某提取器插件或在 `plugins.disabled` 中禁用它,系统 SHALL 不将该提取器计为可用,true
  r113,"preferred-extractor-is-attempted-first","",用户指定 `preferredExtractor=X` 且 X 可用,系统 SHALL 首先尝试 X,true
  r113,"no-extractors-available-produces-a-stable-failure","",系统在当前配置与插件集合下没有任何可用提取器,fetch 模式 SHALL 失败并返回稳定的错误码与恢复提示（例如“安装/启用官方 extractor 插件”）,true
  r150,"frontend-displays-extractor-diagnostics","",前端请求提取器清单端点,响应 SHALL 包含每个提取器的 enabled/available 状态,true
  r185,"user-disables-an-extractor-for-a-notebook","",用户在 UI 中对某 notebook 关闭某 extractor,后端 SHALL 持久化该覆盖并在 extractor 清单中反映 `enabled=false`,true
  r185,"notebook-overrides-are-writable-via-api","","客户端通过 `PATCH /v2/notebooks/{notebookId}/sources/extractors` 更新某 notebook 的 extractor 策略（mode 与启用/禁用覆盖）",后端 SHALL 持久化该覆盖,true
  r217,"retryable-extractor-errors-are-detected","",提取器因网络/服务不可用导致失败,系统 SHALL 能将该错误判定为可重试并按策略重试,true
  "extractors-full-response","client-lists-extractors",客户端请求 GET /extractors,构建响应,系统 SHALL 返回每提取器的 available/displayName/priority/requiresApiKey/recoveryHint 字段,true
  r_extractor_outbound_proxy,"jina-firecrawl-use-outbound-fetch","",proxy_settings.enabled=true,Jina/Firecrawl 出站 SHALL 经 outboundFetch 注入代理,true
