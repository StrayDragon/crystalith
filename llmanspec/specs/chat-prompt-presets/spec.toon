kind: llman.sdd.spec
name: "chat-prompt-presets"
purpose: "定义 Chat 输入中的 `/prompt:*` 指令与后端 preset registry（built-in + custom）的稳定行为：如何解析指令、如何选择生成策略，以及各 preset 的最小输出契约（包含纯文本回答与可选的 session `sharedState.ui` 更新）。"
valid_scope[2]: src/,tests/
requirements[9]{req_id,title,statement}:
  r25,Prompt directives are parsed from QA question when enabled,"当 `app.features.chat_prompt_presets_enabled=true` 时，系统 MUST 支持在 `question` 内解析 prompt 指令： `/prompt:<preset> <query>` 其中： - `<preset>` MUST 匹配正则：`[a-z0-9_-]{1,32}`（大小写不敏感，解析后统一为小写） - `<query>` MAY 为空（空时视为输入错误）"
  r83,Unknown preset names are rejected with a stable list,"系统 MUST 将 preset 集合视为白名单（built-in + custom）。未知 preset MUST 被拒绝并返回可用 preset 列表。"
  r121,Custom prompt presets can override the QA system prompt,"系统 MUST 支持用户定义的 custom preset。对于 `/prompt:<preset> <query>`： - 若 `<preset>` 命中 custom preset 且 `enabled=true`，系统 MUST 使用该 preset 的 `systemPrompt` 覆盖 QA pipeline 的 system message。 - 系统 MUST 将 `<query>` 作为实际 QA question 执行（不包含 `/prompt:` 前缀）。"
  r157,Disabled presets are rejected deterministically,当 preset 存在但 `enabled=false` 时，系统 MUST 拒绝该请求并返回确定性错误。
  r192,Stats preset returns structured JSON and fallback markdown,"当选择 `stats` preset 时，系统 MUST 使用受控的模型输出格式：模型最终输出 MUST 为单个 JSON object（无额外文本），且 MUST 满足： - `fallbackMarkdown: string`（MUST 非空，且 SHOULD 含 inline citations，如 `[1]`） - `chart: { title: string, unit?: string, items: [{ label: string, value: number }] }` - `table?: { columns: string[], rows: (string|number|null)[][] }`"
  r223,Stats structured output is validated and falls back safely,系统 MUST 对 stats JSON 做结构校验；若解析或校验失败，系统 MUST 回退到默认 QA 文本生成（并保持对话可用）。
  r248,Stats persists plain answer text and shared UI state,"当 `stats` preset 生成合法的结构化结果时，系统 MUST： - 将 `fallbackMarkdown` 持久化为 assistant `content` - 通过 session `sharedState.ui` 提供图表/表格 mounts - 不再依赖 `chat_ui_envelope_enabled` 或在 `content` 中嵌入 envelope"
  "preset-inline-directive","QA MUST parse /prompt: directive from question text","QA MUST 从 question 文本解析 /prompt:<preset> 前缀指令（如 /prompt:stats <query>），MUST NOT 只接受 body preset 字段"
  "preset-stats",QA MUST support stats preset with chart and table JSON,QA MUST 支持 stats preset，产出 chart+table 结构化 JSON（STATS_SYSTEM_PROMPT + StatsChart/StatsTable schema），MUST NOT 缺失该 preset
scenarios[11]{req_id,id,given,when,then,feature}:
  r25,"directive-selects-a-preset","","`question` 以 `/prompt:stats ` 开头且 presets 功能已启用",系统 SHALL 选择 `stats` preset 的生成策略而不是默认 QA 策略,true
  r25,"empty-query-returns-deterministic-error","","`question` 为 `/prompt:stats`（无 query）且 presets 功能已启用",系统 SHALL 返回 400 并提示用法与可用 preset 列表,true
  r25,"presets-disabled-returns-deterministic-error","","`question` 以 `/prompt:` 开头但 `app.features.chat_prompt_presets_enabled=false`",系统 SHALL 返回 400（不应静默当作普通 QA 处理）,true
  r83,"unknown-preset-is-rejected","","`question` 为 `/prompt:unknown do something` 且 presets 功能已启用","系统 SHALL 返回 400 并包含可用 preset 名称列表（包含 built-in 与 custom）",true
  r121,"custom-preset-overrides-system-prompt","",用户存在 custom preset `demo` 且 `enabled=true`,系统 SHALL 以 `demo.systemPrompt` 作为 system message 执行 QA,true
  r157,"disabled-preset-returns-400","",用户存在 preset `demo` 且 `enabled=false`,系统 SHALL 返回 400,true
  r192,"stats-preset-returns-json-only","",系统执行 `stats` preset,系统 SHALL 要求模型仅输出 JSON object（不含 code fence、解释文本或多段输出）,true
  r223,"invalid-json-falls-back-to-text-qa","",模型输出无法被解析/校验为 stats JSON,系统 SHALL 回退生成纯文本回答并正常返回 citations/evidence/confidence,true
  r248,"stats-returns-mounts-through-shared-state","",`stats` preset 执行完成且结构化输出校验通过,assistant 消息 `content` SHALL 仅包含 `fallbackMarkdown`,true
  "preset-inline-directive","question-has-directive","question 文本为 /prompt:stats 销售数据",解析阶段,系统 SHALL 提取 preset=stats 并以剩余文本作为 query,true
  "preset-stats","client-uses-stats",客户端选择 stats preset,生成阶段,系统 SHALL 产出 chart+table 结构化 JSON,true
