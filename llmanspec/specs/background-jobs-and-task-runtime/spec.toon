kind: llman.sdd.spec
name: "background-jobs-and-task-runtime"
purpose: 提供统一的后台任务运行时模型：生命周期、进度、事件与历史、取消与重试语义，以及创建/查询/操作的最小接口契约，供生成、导入、同步检查等长任务复用。
valid_scope[2]: src/,tests/
requirements[6]{req_id,title,statement}:
  r23,后台任务必须使用统一生命周期,系统 MUST 以统一的后台任务生命周期表达长时操作，而不是让每个业务流程各自定义状态集合。
  r81,后台任务必须暴露进度、事件与历史,系统 MUST 为后台任务提供可查询的进度、关键事件与历史记录语义。
  r119,后台任务必须支持显式取消与重试,系统 MUST 把取消与重试定义为后台任务的稳定动作，而不是依赖客户端自行重建流程。
  r156,系统必须提供任务的创建、查询、取消、重试与历史查看接口语义,系统 MUST 提供稳定的任务运行时接口，使不同业务流程都能以同一方式使用后台任务底座。
  r191,任务结果必须提供结构化摘要与恢复提示,系统 MUST 为任务提供结构化结果摘要与错误摘要，并在失败时提供明确的恢复提示以支持统一 UI 展示。
  r222,工作区必须提供统一入口查看任务状态与历史,系统 MUST 在工作区提供统一入口查看后台任务列表、状态、进度与历史记录，避免每条长链路各自实现一套任务 UI。
scenarios[8]{req_id,id,given,when,then,feature}:
  r23,创建并执行一个长任务,"",系统启动一个需要异步执行的任务,该任务 SHALL 进入统一生命周期,true
  r81,客户端查询任务执行状态,"",客户端查看某个后台任务,系统 SHALL 返回任务当前状态、进度摘要和关键事件,true
  r119,用户取消或重试后台任务,"",用户对一个后台任务执行取消或重试,系统 SHALL 以任务动作为单位处理请求,true
  r156,客户端创建一个后台任务,"",客户端提交一个 JobDefinition 创建任务,系统 SHALL 返回任务标识与初始状态（至少为 `queued`）,true
  r156,客户端查询任务详情与历史,"",客户端查询某个任务,系统 SHALL 返回任务状态、进度摘要、关键事件与结果/错误摘要,true
  r191,任务成功完成,"",某个任务成功完成,系统 SHALL 返回可用于 UI 展示的结果摘要,true
  r191,任务失败完成,"",某个任务失败完成,系统 SHALL 返回结构化错误摘要,true
  r222,用户在工作区查看任务中心,"",用户进入工作区的任务中心,系统 SHALL 展示该工作区的任务列表（可按 status/type 过滤）,true
