kind: llman.sdd.spec
name: bdd-test-harness
purpose: "定义 Crystalith v2 可选中文 BDD：Gherkin runner + bun test，覆盖已对齐的 CRUD 子集；LLM/插件域可 SKIP。BDD 不在 just qa 主门禁内。"
valid_scope[1]: apps/server/tests/bdd/
requirements[4]{req_id,title,statement}:
  r24,中文 BDD 测试解析器,"MUST 提供基于 @cucumber/gherkin 的 BDD runner，MUST 支持 language zh-CN 的 .feature 解析，MUST 与 bun test 集成（BDD 测试目录）。。"
  r82,Feature 文件维护在 v2 树,"MUST 在 BDD features 目录维护中文 .feature；API 路径 MUST 使用 /v2 前缀。活跃域与跳过白名单 MUST 在 BDD 运行器中明示。"
  r120,BDD 为可选回归入口,"MUST 提供 just test-bdd（或等价 BDD 运行命令）。MUST NOT 将 BDD 作为 just qa 或 pre-commit 硬性门禁，直至 skip 集合显著缩小。"
  r_integration_tests,Cross-module integration tests exist,"系统 MUST 在 server 集成测试目录提供关键路径覆盖；just qa MUST 包含该目录的 bun test。"
scenarios[4]{req_id,id,given,when,then}:
  r24,s1,"Gherkin .feature 使用 zh-CN","运行 just test-bdd","系统解析中文关键词并生成 test blocks"
  r82,s2,"features 目录存在多个域的 .feature","检查路径与 SKIP 集合","活跃 CRUD 域使用 /v2；LLM/插件域在 SKIP 中不产生 fail"
  r120,s3,"开发者运行 just qa","观察是否执行 BDD","SHALL 不自动运行 just test-bdd"
  r_integration_tests,qa_unit_dir,"开发者运行 just test","覆盖 apps/server/tests/","SHALL 执行该目录下集成/单元用例"
