kind: llman.sdd.spec
name: "generation-core"
purpose: 定义统一生成主链路：上下文解析、结构化生成、后处理、引用映射、持久化，以及 QA/refine 与 preference 的一致语义。
valid_scope[2]: src/,tests/
requirements[8]{req_id,title,statement}:
  r1,Output generation follows deterministic node pipeline,"结构化输出 MUST 遵循 `ResolveContext -> Generate -> Postprocess -> MapCitations -> Persist` 逻辑顺序。"
  r2,Source scope is explicit and validated,生成/问答请求中的 `source_ids` 语义 MUST 明确且可校验；非法范围 MUST 返回错误。
  r3,"Preference tuning is table-driven",`preference` MUST 通过集中 tuning 表映射到检索与重试默认值。
  r4,Explicit request knobs override defaults,请求显式给定 `top_k/min_score` 等参数时 MUST 覆盖 preference 默认值。
  r5,"QA stream and non-stream completion logic is shared",流式与非流式 QA MUST 复用同一完成阶段逻辑，保证 citations/evidence/confidence 一致。
  r6,Refine keeps stable request/response contracts,refine 单格式与批量路径 MUST 保持稳定字段语义与引用返回结构。
  r7,"Postprocessing is deterministic and citation-safe",后处理 MUST 先于引用映射，且对非法 citation 索引做清洗并保持非阻塞。
  r8,Tool output types require a corresponding OutputTypePlugin,"对于 FAQ/GUIDE/TIMELINE/MINDMAP/QUIZ/BRIEFING 等“工具输出类型”，系统 MUST 要求存在可用的 `OutputTypePlugin` 才能生成结构化输出；当插件缺失/禁用/加载失败时，生成请求 MUST 失败并返回可执行的恢复提示。 错误响应 MUST 使用统一错误信封（`error_code`, `message`, `details?`, `retry_after?`），且： - HTTP status SHOULD 为 409（或等价“能力不可用”语义） - `details` MUST 包含 `output_type` 与推荐的 `required_plugin_id`（例如 `output-faq`） - `message` MUST 可直接面向用户显示"
scenarios[8]{req_id,id,given,when,then}:
  r1,"generation-pipeline-order-is-stable","",系统处理一次结构化输出生成请求,"各阶段 SHALL 按 `ResolveContext -> Generate -> Postprocess -> MapCitations -> Persist` 的顺序执行"
  r2,"invalid-source-scope-is-rejected","",请求携带非法或越权的 `source_ids`,系统 SHALL 返回明确错误并拒绝继续生成
  r3,"preference-maps-to-defaults","",请求选择某个 `preference`,系统 SHALL 从集中 tuning 表加载对应的检索与重试默认值
  r4,"request-knobs-override-tuning","",请求同时提供 `preference` 与显式 `top_k/min_score` 等参数,系统 SHALL 以显式参数覆盖 tuning 默认值
  r5,"stream-and-non-stream-responses-are-consistent","",同一输入分别使用流式与非流式 QA 路径,系统 SHALL 在完成阶段返回一致的 citations/evidence/confidence 语义
  r6,"refine-contract-stays-stable","",调用 refine 的单格式或批量接口,系统 SHALL 返回稳定的字段语义与引用结构
  r7,"invalid-citations-are-sanitized","",模型输出包含非法 citation 索引或越界引用,系统 SHALL 在后处理阶段清洗并保持流程非阻塞
  r8,"missing-plugin-blocks-tool-output-generation","",客户端请求生成某工具输出类型但服务器未安装/未启用对应插件,系统 SHALL 返回失败响应
