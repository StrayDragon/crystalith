kind: llman.sdd.spec
name: "slides-workflow-plugins"
purpose: 定义 slides workflow plugin 的宿主/插件契约：生成阶段边界、active plugin 选择、预览与前端声明模型，以及官方参考插件的稳定样板。
valid_scope[2]: src/,tests/
requirements[3]{req_id,title,statement}:
  r44,"Slides workflow plugin is a first-class contract","宿主 MUST 提供一类独立的 slides workflow plugin 契约，用于承载 slides 的默认配置、outline 生成、markdown 生成、preview 声明与前端 bundle 元数据，而不是将这些能力隐式塞入其他插件接口。 该契约至少 SHOULD 支持： - metadata（显示文案、tone 等） - `configSchema`（defaults + rich options） - `generate_outline(...)` - `generate_markdown(...)` - `previewDescriptor`（若插件支持预览） - `frontendBundle`（若插件提供专用前端渲染）"
  r102,"Slides workflow activation is single-active and deterministic","对 `SLIDES` 业务能力，宿主 MUST 在任一时刻仅选择一个 active slides workflow plugin 对外生效，并保证选择结果可预测、可诊断。 选择规则 MUST 满足： - 若配置了 `slides.default_plugin`，则优先按该 plugin id 选择 - 若未配置且仅发现一个兼容候选，则可自动选择 - 若未配置且候选为 0 或大于 1，则 MUST 返回结构化诊断而不是静默选取"
  r139,Repository provides an official reference slides plugin,"仓库 MUST 提供一个官方 reference plugin，作为“复杂工作流插件”的样板，实现 slides workflow contract 的完整子集并附带安装/启用说明。 该 reference plugin MUST： - 使用稳定命名（如 `slides-slidev`） - 暴露 rich `configSchema`、preview contract 与 frontend bundle metadata - 作为官方文档中的自托管安装示例与二次开发参考"
scenarios[3]{req_id,id,given,when,then,feature}:
  r44,"host-loads-a-slides-workflow-plugin","",宿主发现并加载一个实现 slides workflow contract 的插件,系统 SHALL 将其注册为 slides workflow 候选,true
  r102,"multiple-slides-plugins-require-explicit-selection","",同时安装多个兼容的 slides workflow plugin 且未配置默认项,宿主 SHALL 不对外暴露 active `SLIDES` capability,true
  r139,"official-reference-plugin-exposes-slides-capability","",运维安装并启用官方 reference slides plugin,`/v1/workspace/tools` SHALL 可通过 active plugin 暴露 `SLIDES` tool,true
