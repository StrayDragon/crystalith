kind: llman.sdd.spec
name: "typed-generation-framework"
purpose: 把“生成类型”提升为一等对象：定义最小公共词汇、注册与发现语义、请求/结果的类型绑定，以及对下游扩展的边界与扩展位。
valid_scope[2]: src/,tests/
requirements[7]{req_id,title,statement}:
  r1,系统必须提供生成类型的注册与发现,系统 MUST 以统一方式注册并向调用方发现可用的生成类型集合。
  r2,生成请求必须显式绑定生成类型,系统 MUST 将生成类型作为显式的一等对象，而不是隐含在 prompt 或入口按钮中。
  r3,系统必须按生成类型契约装配请求并做最小校验,系统 MUST 使用生成类型契约装配默认值并做最小输入要求校验，避免类型语义被隐式分叉。
  r4,生成类型必须声明最小类型契约,系统 MUST 为每种生成类型声明最小契约，以支撑后续控制项、结果结构和完成语义。
  r5,生成结果必须回传生成类型、输出类型与完成语义元数据,系统 MUST 在结果对象中同时回传生成类型与输出类型，并回传完成语义相关元数据以支撑治理与后续工作流。
  r6,生成类型与输出类型必须保持边界分离,系统 MUST 保持生成类型与输出类型的边界清晰，避免一个概念吞掉另一个概念。
  r7,下游扩展只能使用扩展位，禁止反向改写公共词汇,系统 MUST 为下游能力提供明确扩展位，同时禁止下游能力反向改写公共词汇定义。
scenarios[9]{req_id,id,given,when,then}:
  r1,系统启动时装载内置生成类型,"",系统启动,系统 SHALL 将内置生成类型注册到统一 registry
  r1,前端查询可用生成类型列表,"",前端需要渲染右侧生成入口,系统 SHALL 提供可用生成类型列表（至少包含 id、display_name、输入要求概览、可选输出类型概览、完成语义概览）
  r2,用户发起某一类生成,"",用户发起一次生成请求,该请求 SHALL 显式指向一个生成类型
  r3,请求未携带生成类型,"",某个生成请求缺失生成类型标识,系统 SHALL 拒绝该请求
  r3,请求未指定输出类型,"",生成请求已指定生成类型但未显式指定输出类型,系统 SHALL 使用该类型契约的默认输出类型装配请求
  r4,系统注册一个生成类型,"",系统新增或加载某个生成类型,该类型 SHALL 至少声明输入要求、输出结构、控制面与完成语义
  r5,生成完成并返回结果,"",某个生成请求完成并返回结果,结果 SHALL 回传其生成类型标识与输出类型标识
  r6,结果被渲染为某种输出形式,"",某个生成结果被渲染或承载,系统 SHALL 能区分其生成类型与输出类型
  r7,下游能力需要为某些生成类型增加控制项或改良动作,"",下游能力需要为部分生成类型增加 knobs/presets 或 refinement 动作,该能力 SHALL 通过公共框架的扩展位表达（control_surface / allows_refinement 等）
