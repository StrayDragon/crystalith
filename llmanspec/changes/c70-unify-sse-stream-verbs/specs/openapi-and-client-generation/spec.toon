kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  add_requirement,"asyncapi-stream-verbs-match-handlers","AsyncAPI stream verbs MUST match handlers","AsyncAPI（及 OpenAPI 中流式端点登记）的 method/path MUST 与 Elysia handler 一致：QA stream POST；research/studio progress stream GET。MUST NOT 文档与实现动词不一致。",null,null,null
op_scenarios[1]{req_id,id,given,when,then,feature}:
  "asyncapi-stream-verbs-match-handlers","research-asyncapi-get","research stream handler 为 GET","检查 AsyncAPI address/method","文档 SHALL 反映 GET 且 path 与 handler 对齐",false
