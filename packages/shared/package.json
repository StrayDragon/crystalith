{
  "name": "@crystalith/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas/index.ts",
    "./types": "./src/types/index.ts"
  },
  "scripts": {
    "lint": "echo '@crystalith/shared lint is managed by root; run bun lint from repo root'",
    "format": "echo '@crystalith/shared format is managed by root; run bun format from repo root'",
    "format:check": "echo '@crystalith/shared format:check is managed by root; run bun format:check from repo root'",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "typesafe-i18n": "typesafe-i18n"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^8.5.0"
  },
  "devDependencies": {
    "typesafe-i18n": "^5.27.1",
    "typescript": "^7.0.2",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "zod": "^4"
  }
}
